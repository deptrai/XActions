# Browser Scripts

The zero-install surface. Copy a script, paste it into your browser's DevTools
console on x.com, and it runs against the page you already have open, using the
session you are already logged into.

No `npm install`, no API key, no cookie extraction, no server. This is the
fastest way to try XActions, and for one-off cleanup jobs it is often the right
tool rather than a fallback.

---

## How to run one

1. Open the page the script expects (each one names it below). For most, that
   is `https://x.com/YOUR_USERNAME/following`.
2. Open DevTools:
   - **Windows/Linux** — <kbd>F12</kbd> or <kbd>Ctrl</kbd>+<kbd>Shift</kbd>+<kbd>I</kbd>
   - **macOS** — <kbd>Cmd</kbd>+<kbd>Option</kbd>+<kbd>I</kbd>
3. Click the **Console** tab.
4. Paste the whole script and press <kbd>Enter</kbd>.

The first time you paste into a console, Chrome asks you to type `allow
pasting`. That is a browser safety feature, not something XActions can skip.

> **Read what you paste.** These scripts act as you, on your account. Every one
> in this repo is short enough to read end to end before you run it, and that is
> deliberate. Never paste a console script from anywhere without reading it.

---

## Start in dry-run

Every destructive script ships with `dryRun: true` at the top of its `CONFIG`
block. In that mode it walks the page, logs exactly what it *would* do, and
changes nothing:

```js
const CONFIG = {
  maxUnfollows: Infinity,   // Cap total unfollows (set to e.g. 50 for testing)
  whitelist: [],            // Usernames to never unfollow (without @)
  dryRun: true,             // Preview without acting — SET FALSE TO RUN
  delay: 2000,              // ms between unfollows
  scrollDelay: 2000,        // ms to wait after scroll
  maxEmptyScrolls: 6,       // Give up after N scrolls with no new users
  exportOnComplete: true,   // Auto-download JSON results
};
```

Run it once as-is, read the output, then set `dryRun: false` and run it again.
Skipping this step is how people unfollow accounts they meant to keep.

### The settings worth changing

| Setting | Why you would change it |
|---------|-------------------------|
| `dryRun` | `true` to preview, `false` to act. Always preview first. |
| `whitelist` | Usernames to never touch. Add friends, clients, and your alt account before a bulk run. |
| `maxUnfollows` | Cap the run. Start at `50` to see how X reacts to your account before letting it loose. |
| `delay` | Milliseconds between actions. Lower is faster and likelier to hit a rate limit. See below. |
| `exportOnComplete` | Downloads a JSON record of everything the script did. Keep this on. |

---

## Rate limits, and not getting locked out

X does not publish its automation limits, and they differ by account age,
follower count, and verification status. What holds in practice:

- **Keep `delay` at 2000ms or higher.** The defaults in this repo are chosen to
  look like a fast human rather than a script.
- **Stay under a few hundred write actions per day.** Unfollows, follows, likes,
  and deletes all draw on the same budget.
- **Stop when actions start failing silently.** A button that no longer responds
  means you are being throttled. Continuing turns a soft throttle into a
  temporary lock.
- **Spread big jobs over days.** A 5,000-account cleanup is a week of runs, not
  an afternoon.

Scripts print their progress as they go. If the count stops climbing while the
page keeps scrolling, stop the run.

### Stopping a run

Reload the page. Every script is scoped to the page it runs on and holds no
state elsewhere, so a refresh ends it immediately. Scripts with
`exportOnComplete` write their JSON when they finish normally, so a run you
abort early will not produce one.

---

## Command line and library alternatives

The same jobs are available with more control from the CLI and the Node.js
library, which stream results, resume, and export to CSV or Google Sheets:

```bash
npx xactions profile nasa
npx xactions non-followers YOUR_USERNAME
npx xactions tweets nasa --limit 200 --output tweets.csv
```

See the [CLI reference](cli-reference.md), the [examples](../examples/), and the
[getting started guide](getting-started.md).

---

## The catalog

<!-- AUTOGEN:SCRIPTS start -->

_93 scripts. Generated by `npm run docs:scripts` from the header of each file in [`scripts/`](../scripts/)._

| Script | What it does | Run it on |
|--------|--------------|-----------|
| [`accountHealthMonitor`](../scripts/accountHealthMonitor.js) | Computing a 0-100 account health score | `x.com/USERNAME` |
| [`algorithmBuilder`](../scripts/algorithmBuilder.js) | Training X's algorithm by engaging with niche content | `x.com/home or x.com/search` |
| [`articlePublisher`](../scripts/articlePublisher.js) | Publishing long-form articles on X (Premium+ required) | `x.com/compose/article` |
| [`audienceDemographics`](../scripts/audienceDemographics.js) | Analyzing follower bios, niches, locations, verified status | `x.com/USERNAME/followers` |
| [`audienceOverlap`](../scripts/audienceOverlap.js) | Analyzing shared followers between X accounts | `x.com/ACCOUNT/followers` |
| [`auditFollowers`](../scripts/auditFollowers.js) | Auditing follower quality on X/Twitter | `x.com/USERNAME/followers` |
| [`autoEngage`](../scripts/autoEngage.js) | Automated engagement on X/Twitter | `x.com/home or any feed` |
| [`autoPlugReplies`](../scripts/autoPlugReplies.js) | Auto-replying to your viral tweets with a plug | `x.com/YOUR_USERNAME` |
| [`autoReply`](../scripts/autoReply.js) | Auto-replying to tweets matching keyword triggers | `x.com/home or any timeline/search` |
| [`autoRepost`](../scripts/autoRepost.js) | Auto-reposting tweets matching keywords or users | `x.com/home or any timeline/search` |
| [`backupAccount`](../scripts/backupAccount.js) | Backing up your X/Twitter profile and tweets as JSON | `x.com/YOUR_USERNAME` |
| [`bestTimeToPost`](../scripts/bestTimeToPost.js) | Finding optimal posting times via engagement heatmap | `x.com/USERNAME` |
| [`blockBots`](../scripts/blockBots.js) | Detecting and blocking bot accounts on X/Twitter | `x.com/USERNAME/followers` |
| [`bookmarkManager`](../scripts/bookmarkManager.js) | Exporting and clearing bookmarks on X/Twitter | `x.com/i/bookmarks` |
| [`bookmarkOrganizer`](../scripts/bookmarkOrganizer.js) | Categorizing bookmarks by keywords on X/Twitter | `x.com/i/bookmarks` |
| [`bulkDeleteTweets`](../scripts/bulkDeleteTweets.js) | Bulk deleting tweets with filters | `x.com/YOUR_USERNAME (your profile)` |
| [`businessAnalytics`](../scripts/businessAnalytics.js) | X/Twitter business analytics and brand monitoring | `x.com or business.x.com` |
| [`businessTools`](../scripts/businessTools.js) | Brand mention monitoring and sentiment analysis | `x.com/search or any timeline` |
| [`clearAllBookmarks`](../scripts/clearAllBookmarks.js) | Clearing all bookmarks on X/Twitter | `x.com/i/bookmarks` |
| [`clearAllReposts`](../scripts/clearAllReposts.js) | Removing all reposts/retweets from your profile | `x.com/YOUR_USERNAME (your profile)` |
| [`competitorAnalysis`](../scripts/competitorAnalysis.js) | Comparing competitor account stats and engagement | `x.com (any page)` |
| [`contentCalendar`](../scripts/contentCalendar.js) | Analyzing posting patterns and schedule visualization | `x.com/USERNAME (your profile)` |
| [`contentRepurposer`](../scripts/contentRepurposer.js) | Repurposing top tweets into threads, storms, blog outlines | `x.com/YOUR_USERNAME` |
| [`continuousMonitor`](../scripts/continuousMonitor.js) | Continuous auto-refresh monitoring of followers/following | `x.com/USERNAME/followers or /following` |
| [`createPoll`](../scripts/createPoll.js) | Creating a poll tweet on X/Twitter | `x.com` |
| [`creatorStudio`](../scripts/creatorStudio.js) | Analyzing account analytics and content performance | `x.com/USERNAME (your profile)` |
| [`detectUnfollowers`](../scripts/detectUnfollowers.js) | Detecting who unfollowed you on X/Twitter | `x.com/USERNAME/followers` |
| [`discoveryExplore`](../scripts/discoveryExplore.js) | Scraping trending topics and search results on X/Twitter | `x.com/explore` |
| [`dmExporter`](../scripts/dmExporter.js) | Browser console script to export X/Twitter DM conversations | `x.com/messages` |
| [`dmManager`](../scripts/dmManager.js) | Sending DMs and exporting conversations on X/Twitter | `x.com/messages` |
| [`downloadAccountData`](../scripts/downloadAccountData.js) | Triggering X/Twitter's official data archive download | `x.com/settings/download_your_data` |
| [`editProfile`](../scripts/editProfile.js) | Browser console script to update X/Twitter profile fields | `x.com/settings/profile` |
| [`engagementAnalytics`](../scripts/engagementAnalytics.js) | Scraping engagement metrics from a profile | `x.com/USERNAME` |
| [`engagementBooster`](../scripts/engagementBooster.js) | Boosting engagement — like & reply to matching tweets | `x.com/home or any timeline/search` |
| [`engagementLeaderboard`](../scripts/engagementLeaderboard.js) | Tracking who engages most with your content | `x.com/YOUR_USERNAME` |
| [`engagementManager`](../scripts/engagementManager.js) | Liking, bookmarking, or replying to visible tweets | `x.com/home or any timeline` |
| [`followerGrowthTracker`](../scripts/followerGrowthTracker.js) | Tracking follower growth over time on X/Twitter | `x.com/USERNAME (any profile page)` |
| [`followRatioManager`](../scripts/followRatioManager.js) | Analyzing and optimizing your follow/following ratio | `x.com/YOUR_USERNAME` |
| [`grokIntegration`](../scripts/grokIntegration.js) | Interacting with Grok AI on X | `x.com/i/grok` |
| [`hashtagAnalytics`](../scripts/hashtagAnalytics.js) | Analyzing hashtag frequency and engagement | `x.com/search or any timeline` |
| [`joinCommunities`](../scripts/joinCommunities.js) | Discovering and joining X communities by keyword | `x.com/i/communities/suggested` |
| [`keywordLiker`](../scripts/keywordLiker.js) | Like only posts containing specific keywords — with a prompt input box | `x.com/home or any feed/search page` |
| [`keywordMonitor`](../scripts/keywordMonitor.js) | Monitoring timeline keywords and alerting on matches | `x.com/search?q=keyword or x.com/home` |
| [`leaveAllCommunities`](../scripts/leaveAllCommunities.js) | Leaving all X communities | `x.com/USERNAME/communities` |
| [`listManager`](../scripts/listManager.js) | Creating and managing X/Twitter lists | `x.com/i/lists` |
| [`manageBookmarks`](../scripts/manageBookmarks.js) | Browser console script to export and manage X/Twitter bookmarks | `x.com/i/bookmarks` |
| [`manageLists`](../scripts/manageLists.js) | Browser console script to manage X/Twitter Lists | `x.com/i/lists` |
| [`manageMutedWords`](../scripts/manageMutedWords.js) | Bulk-adding muted words on X/Twitter | `x.com/settings/muted_keywords` |
| [`manageNotifications`](../scripts/manageNotifications.js) | Browser console script to scrape and manage X/Twitter notifications | `x.com/notifications` |
| [`manageSettings`](../scripts/manageSettings.js) | Browser console script to export and audit X/Twitter settings | `x.com/settings` |
| [`massBlock`](../scripts/massBlock.js) | Mass blocking users on X/Twitter | `x.com (followers, following, search, or any page with user cells)` |
| [`massUnblock`](../scripts/massUnblock.js) | Mass unblocking users on X/Twitter | `x.com/settings/blocked` |
| [`massUnmute`](../scripts/massUnmute.js) | Mass unmuting users on X/Twitter | `x.com/settings/muted` |
| [`monitorAccount`](../scripts/monitorAccount.js) | Tracking follows/unfollows on any public account | `x.com/USERNAME/followers or /following` |
| [`multiAccountTimelineLiker`](../scripts/multiAccountTimelineLiker.js) | Like the entire timeline of multiple X accounts in sequence | `x.com` |
| [`muteByKeywords`](../scripts/muteByKeywords.js) | Muting users by tweet keywords on X/Twitter | `x.com (any timeline or search page)` |
| [`newFollowersAlert`](../scripts/newFollowersAlert.js) | Detecting new followers on X/Twitter | `x.com/USERNAME/followers` |
| [`notificationManager`](../scripts/notificationManager.js) | Scraping and filtering notifications on X/Twitter | `x.com/notifications` |
| [`personaEngine`](../scripts/personaEngine.js) | Analyzing your posting style and generating a persona profile | `x.com/YOUR_USERNAME` |
| [`pinTweetManager`](../scripts/pinTweetManager.js) | Pinning your best-performing tweet | `x.com/YOUR_USERNAME (your profile)` |
| [`pollCreator`](../scripts/pollCreator.js) | Browser console script to create polls on X/Twitter | `x.com/compose/tweet` |
| [`postComposer`](../scripts/postComposer.js) | Browser console script to compose and post tweets, threads, and polls | `x.com` |
| [`postThread`](../scripts/postThread.js) | Posting a thread of connected tweets on X/Twitter | `x.com` |
| [`premiumFeatures`](../scripts/premiumFeatures.js) | Browser console script to check X/Twitter Premium status and features | `x.com` |
| [`premiumManager`](../scripts/premiumManager.js) | Detecting Premium tier and features on X/Twitter | `x.com` |
| [`profileManager`](../scripts/profileManager.js) | Scraping profile data from any X/Twitter profile page | `x.com/USERNAME` |
| [`publishArticle`](../scripts/publishArticle.js) | Publishing articles on X/Twitter (Premium+) | `x.com/compose/article` |
| [`qrCodeSharing`](../scripts/qrCodeSharing.js) | Generating a QR code for any X/Twitter profile | `x.com/USERNAME` |
| [`quoteTweetAutomation`](../scripts/quoteTweetAutomation.js) | Auto quote-tweeting matching tweets with templates | `x.com/home or any timeline/search` |
| [`removeFollowers`](../scripts/removeFollowers.js) | Removing followers via soft-block on X/Twitter | `x.com/USERNAME/followers` |
| [`reportSpam`](../scripts/reportSpam.js) | Reporting spam accounts on X/Twitter | `x.com (any page)` |
| [`schedulePosts`](../scripts/schedulePosts.js) | Scheduling posts using X/Twitter's native scheduler | `x.com` |
| [`scrapeAnalytics`](../scripts/scrapeAnalytics.js) | Browser console script to scrape X/Twitter analytics data | `x.com/i/account_analytics or any post` |
| [`scrapeExplore`](../scripts/scrapeExplore.js) | Browser console script to scrape Explore page trends and content | `x.com/explore` |
| [`scrapeSpaces`](../scripts/scrapeSpaces.js) | Browser console script to scrape X/Twitter Spaces data | `x.com/i/spaces or a Space page` |
| [`sendDirectMessage`](../scripts/sendDirectMessage.js) | Sending personalized DMs to multiple users on X/Twitter | `x.com/messages` |
| [`sentimentAnalyzer`](../scripts/sentimentAnalyzer.js) | Analyzing tweet sentiment using a basic lexicon | `x.com (any timeline or search)` |
| [`settingsManager`](../scripts/settingsManager.js) | Exporting settings and toggling protection on X/Twitter | `x.com/settings` |
| [`shadowbanChecker`](../scripts/shadowbanChecker.js) | Checking if your X/Twitter account is shadowbanned | `x.com (any page, logged in)` |
| [`spacesManager`](../scripts/spacesManager.js) | Scraping live and scheduled Spaces on X/Twitter | `x.com/i/spaces or x.com/explore` |
| [`threadComposer`](../scripts/threadComposer.js) | Interactive thread composing with draft persistence | `x.com` |
| [`trendingTopicMonitor`](../scripts/trendingTopicMonitor.js) | Tracking trending topics and detecting rising trends | `x.com/explore/tabs/trending` |
| [`tweetABTester`](../scripts/tweetABTester.js) | A/B testing tweet variants and comparing engagement | `x.com` |
| [`tweetPerformance`](../scripts/tweetPerformance.js) | Ranking tweets by engagement and categorizing by type | `x.com/USERNAME` |
| [`tweetPriceCorrelation`](../scripts/tweetPriceCorrelation.js) | Browser console script — correlate a founder's tweet frequency with token price movements | `x.com/USERNAME (any crypto founder's profile)` |
| [`tweetScheduleOptimizer`](../scripts/tweetScheduleOptimizer.js) | Generating an optimal weekly posting schedule | `x.com/USERNAME` |
| [`unfollowback`](../scripts/unfollowback.js) | Unfollowing non-followers on X/Twitter | `x.com/USERNAME/following` |
| [`unfollowEveryone`](../scripts/unfollowEveryone.js) | Unfollowing everyone on X/Twitter | `x.com/USERNAME/following` |
| [`unfollowWDFBLog`](../scripts/unfollowWDFBLog.js) | Unfollowing non-followers with detailed logging | `x.com/USERNAME/following` |
| [`unlikeAllPosts`](../scripts/unlikeAllPosts.js) | Unliking all your liked posts on X/Twitter | `x.com/YOUR_USERNAME/likes` |
| [`updateProfile`](../scripts/updateProfile.js) | Updating your X/Twitter profile fields | `x.com/YOUR_USERNAME` |
| [`viralTweetDetector`](../scripts/viralTweetDetector.js) | Detecting tweets with unusually high engagement (viral) | `x.com (any timeline or search)` |
| [`welcomeNewFollowers`](../scripts/welcomeNewFollowers.js) | Welcoming new followers with DMs on X/Twitter | `x.com/USERNAME/followers` |

<!-- AUTOGEN:SCRIPTS end -->

---

## Writing your own

Browser scripts in this repo follow one shape, and matching it keeps the
catalog above accurate (it is generated from these headers):

```js
// scripts/myScript.js
// One line describing what it does
// Paste in DevTools console on x.com/home
// by yourhandle

(() => {
  const CONFIG = {
    dryRun: true,
    delay: 2000,
  };

  const sleep = (ms) => new Promise((r) => setTimeout(r, ms));

  // ... your logic
})();
```

Rules that keep a script safe to publish:

- Wrap everything in an IIFE so it cannot collide with page globals.
- Put every tunable value in a `CONFIG` object at the top, not inline.
- Default `dryRun` to `true` for anything that writes.
- Never hardcode a delay below 1000ms.
- Log progress. A silent script is indistinguishable from a hung one.

Then regenerate the catalog:

```bash
npm run docs:scripts
```

See [CONTRIBUTING.md](../CONTRIBUTING.md).

---

## Related

- [Getting Started](getting-started.md) — install the CLI and library
- [CLI Reference](cli-reference.md) — the same operations from a terminal
- [Troubleshooting](troubleshooting.md) — when a script finds nothing
- [Examples](../examples/) — runnable Node.js programs
