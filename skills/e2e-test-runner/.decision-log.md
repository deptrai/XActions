---
phase: complete
classification: simple-workflow
last_touched: 2026-06-13
---

# Decision Log — e2e-test-runner

## Session 2026-06-13 (initial build)

**Classification:** Simple Workflow — multi-stage nhưng inline trong SKILL.md (~76 lines), không cần carve-out.

**Customizable:** No — fixed paths/tools. Users cần override thì fork.

**Platform detection table:** Hai signal (mobile indicators → Appium MCP, web indicators → Chrome DevTools MCP). Khi cả hai có mặt: hỏi user. Lý do: ambiguity thực tế (React Native web + mobile cùng repo); đoán sai = waste cả session setup.

**Automation tools:**
- Appium MCP (không dùng raw Appium CLI) — MCP tools đã có trong session, không cần setup thêm.
- Chrome DevTools MCP (không dùng Playwright) — cùng lý do; user đã cài theo project MCP config.

**Fix loop cap: 3 attempts/step** trước khi escalate. Tránh infinite loop khi failure mang tính structural (selector sai, URL pattern sai) sẽ không resolve bằng cùng approach.

**Seed/infra gộp vào Stage 1** (không tách Stage 0): splitting "find seed" và "start server" ra 2 stages thêm ceremony mà không có giá trị.

**Không dùng Decision-Log Workspace pattern:** Skill này là pure execution, không có revisable artifact. Decision log này chỉ tồn tại vì bmad-workflow-builder yêu cầu; không tạo `.decision-log.md` khi skill chạy.

**Headless mode:** Không support — E2E testing vốn interactive (user phải confirm seed setup, test accounts, production-data warnings).
