---
epic: 32
date: 09-21-2026
verdict: accepted
criteria: declared
headless: false
---

# Retrospective — Epic 32

## Epic summary

Epic 32 (32.1 RateBudgetDashboard+Panic Stop, 32.2 DistributedTokenBucket) commits 1259ae4a+f191878f: governor methods (panicStop/resumePanic/setConsumerPriority), 3 POST routes authenticateToken+requireAdmin, dashboard UI (throttle gauge, RPM per-consumer, drag-drop queue, Panic Stop modal, localStorage priority order), DistributedTokenBucket Lua atomic + fallback in-memory + parseRateLimitHeaders đa chuẩn, re-export core. 19/19 tests pass. Khiếm khuyết: (1) canConsume async nhưng canConsumerRequest trả Promise cho caller đồng bộ → REDIS_TOKEN_BUCKET=1 → quota fail-open; (2) globalAdaptiveRateGovernor chỉ proxyPool, không distributedBucket → flag vô hiệu ngoài test; (3) spec hứa canAccountRequest delegation nhưng chỉ consumer-level; (4) AC per-account RPM nhưng dashboard chỉ per-consumer; (5) test contention mô phỏng 1 process; (6) panic state write-only Redis, không đọc lại khi restart.

## Findings (aggregate views)

- **[Scope & Delivery] (accept)** 2 story giao đủ thành phần chính: governor methods, REST routes admin-protected, dashboard UI (gauge/RPM/drag-drop/panic modal), token bucket Lua atomic, header parser đa chuẩn, re-export core.
  - Source: `git show 1259ae4a/f191878f --stat; adaptive-governor.js:692,738,776; api/routes/governor.js:46,75,103; dashboard/admin.html:889-924,2108-2202; distributed-token-bucket.js:33-77`
- **[Correctness] (fix now)** BUG latent: canConsume async (Promise) nhưng canConsumerRequest nhánh delegation 'return this.#distributedBucket.canConsume(...)' — caller đồng bộ (base-client.js, mcp/server.js) thấy '!Promise' luôn false → mọi request được phép (fail-open) khi REDIS_TOKEN_BUCKET=1. Test delegation mock+await che bug.
  - Source: `distributed-token-bucket.js:256 async canConsume; adaptive-governor.js:209-217,232-244; base-client.js; mcp/server.js; tests/core/distributed-token-bucket.test.js:181-195`
- **[Production Wiring] (fix now)** globalAdaptiveRateGovernor khởi tạo chỉ {proxyPool} — #distributedBucket/#redis null → REDIS_TOKEN_BUCKET=1 production là no-op. globalDistributedTokenBucket tồn tại nhưng không nối vào global governor. Distributed mode hiện dead code ngoài test.
  - Source: `adaptive-governor.js:819; distributed-token-bucket.js:1192`
- **[AC Gap] (defer)** Spec 32.2 hứa ủy quyền 'canConsumerRequest, recordConsumerRequest, canAccountRequest, recordAccountRequest' nhưng chỉ delegate 2 method consumer-level; canAccountRequest không có nhánh distributed. AC 32.1 'per-account RPM' không trong dashboard (chỉ per-consumer).
  - Source: `spec-32-2 Approach; adaptive-governor.js:462-470; dashboard/admin.html:917-924`
- **[Test Coverage & Durability] (fix now)** 19/19 pass (2 files, 660ms). Nhưng 'multi-process contention' chỉ concurrent Promise 1 process + Redis eval mock — chưa test Redis thật. Panic state không bền: hibernate qua #accountRequestTimestamps + key 'xact:panic:{platform}' SET không đọc lại → restart/multi-instance mất critical state; wildcard '{platform}:*' hibernate nhưng isHibernating exact-match nên wildcard không chặn account cụ thể.
  - Source: `distributed-token-bucket.test.js:118-150,170-195; adaptive-governor.js:692-721`
- **[Process & Artifacts] (accept)** sprint-status epic-32=done, retrospective=optional; 2 spec file frozen intent đầy đủ, review triage (32.1 6/6, 32.2 13/13). Commit message khớp test count. Không retro Epic 32 trước đó.
  - Source: `sprint-status.yaml:209-212; spec-32-1/spec-32-2; git log`

## Behavior verification

REDIS_TOKEN_BUCKET chưa bật (production hiện): in-memory sliding window, dashboard+panic local đúng Epic 20/AD-20. Bật REDIS_TOKEN_BUCKET=1 instance hiện: không gì đổi (bucket null→flag vô hiệu). Bật sau khi wiring bucket: quota consumer fail-open mọi caller đồng bộ (bug async), consume ghi fire-and-forget song song in-memory (double book-keeping vô hại), fallback in-memory khi Redis lỗi đúng Never-fail-safe. Panic stop hiệu lực tức thời trong process.

## Previous-retro follow-through

Epic 32 nằm trong nhóm `optional` retrospective; không có prior action item nào của epic này cần follow-through (các action items mở thuộc Epic 20/25/35 đã được xử lý riêng).

## Action items

- Sửa async/sync mismatch: tách canConsumeSync cho in-memory path hoặc chuyển nhánh delegation canConsumerRequest sang check đồng bộ; thêm test mô phỏng caller đồng bộ kiểu base-client khóa hồi quy. Chặn bật flag production tới khi xong. *(owner: dev)*
- Wiring: nối globalAdaptiveRateGovernor với globalDistributedTokenBucket + redis client (đọc REDIS_TOKEN_BUCKET/REDIS_URL lúc bootstrap); nếu chưa bật thì disable rõ (log warn khi flag set mà thiếu bucket). *(owner: dev)*
- Đọc lại 'xact:panic:*' khi governor khởi động (và khi canConsumerRequest) để khôi phục panic sau restart/đa instance; xử lý wildcard '{platform}:*' trong isHibernating (prefix match) hoặc bỏ wildcard entry. *(owner: dev)*
- Quyết rõ 32.2/32.1: bổ sung delegation account-level (canAccountRequest/recordAccountRequest) + cột Per-Account RPM dashboard, HOẶC ghi descope chính thức vào spec. *(owner: dev)*
- Thay/nâng test 'multi-process contention' bằng test Redis thật (ioredis+testcontainer hoặc redis-server local) 2+ process/concurrent eval xác nhận Lua atomic; giữ mock cho fallback. *(owner: dev)*

## Pending / gated

- Không có.

## Acceptance verdict

**accepted** (criteria: declared).

## Open questions

- canConsumerRequest nên chuyển hẳn sang async API (breaking change base-client+mcp) hay giữ sync + local-cache snapshot Redis refresh định kỳ?
- Per-account RPM + account-level delegation: descope thành story 32.3 hay vá ngay sprint này?
