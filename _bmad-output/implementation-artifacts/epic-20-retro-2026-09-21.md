---
epic: 20
date: 09-21-2026
verdict: accepted
criteria: declared
headless: false
---

# Retrospective — Epic 20: Multi-Consumer Scraping Platform Service Contract

## Epic summary

- **Epic:** 20 — expose `scrape()` dispatcher thành service-to-service contract cho multi-consumer (Nowing, ChainLens, AI agents). Control plane qua MCP `x_scrape`, data plane qua Redis Stream `stream:social:raw_posts` (snake_case ThinEvent), discovery qua `x_actions_list`.
- **Stories completed:** `20-1` (x_scrape + x_actions_list + Action Matrix), `20-2` (Universal Stream-Publish Hook / snake_case ThinEvent + mapToThinEvent). External milestones 20.3/20.4 tracked ở Nowing repo — không thuộc scope repo này.
- **pending_stories:** `[]` — sạch, cả 2 story `done`.
- **Diff range / commits:**
  - `20-1` → `206065b3` "feat(mcp): Story 20.1 — multi-consumer service contract" (baseline `4a6db23c`)
  - `20-2` → stream-publish hook được implement rải rác: `9d924dfa` (guard `__streamEmitted` + got-jsdom), `3673757a` (Story 38.1 Crawler Lifecycle + Stream Unification), `1dc3c03e` (Story 38.2 CloudEvents + Idempotent Publishing). Story 20.2's universal hook nằm trong `AbstractCrawler.#emitStreamEvents` (src/core/base-crawler.js:220+).
- **Evidence inventory:** spec files `spec-20-1-*.md` + `spec-20-2-*.md` (có frozen-after-approval ACs), `epic-20-context.md`, `story-20-1-review.diff` (5482 dòng), `docs/canonical-action-matrix.{md,json}`, tests `tests/mcp/x-scrape-tool.test.js`, `tests/mcp/actions-list-complete.test.js`, `tests/mcp/envelope.test.js`, `tests/docs/matrix-generation.test.js`. Session logs: không có (gap — ghi nhận, không phải lỗi).

## Findings (aggregate views)

- **Spec-to-implementation reconciliation:** Không phát hiện divergence blocking. Story 20.2 spec yêu cầu "gỡ bỏ per-crawler direct emit → delegate sang base hook" — đã xảy ra nhưng **theo hướng consolidation sang Epic 38** (`3673757a`/`1dc3c03e`), không phải trong commit 20.x riêng. Đây là *accepted deviation*: work được hoàn thành nhưng phân bổ lại vào Story 38.1/38.2 (stream unification). Record để không re-flag.
- **Architecture delta:** `x_scrape` thêm control-plane MCP mới; stream-publish hook tập trung vào `AbstractCrawler` thay vì rải rác per-crawler — đúng hướng (giảm duplication).
- **God-class growth:** `story-20-1-review.diff` 5482 dòng là review artifact, không phải source growth. `src/mcp/server.js` +296 dòng — lớn nhưng là tool-definition/handler phù hợp, không phải god-class mới.
- **Duplication:** `9d924dfa` fix duplicate thin-event emission (`__streamEmitted` guard) — đã vá, ghi nhận là defect đã-fix-in-epic.

## Behavior verification

Runtime end-to-end không được exercise trong retro này (Nowing shadow-run 20.3 là external milestone). Evidence: `x_scrape` tool test 191 dòng, actions-list test 76 dòng, matrix JSON auto-generated 3679 dòng tồn tại trên disk — artifacts của implementation có thật.

## Previous-retro follow-through

Epic 20 là epic đầu của "Phase 4 extension" stream/dispatcher; không có prior-epic action item nào trong `sprint-status.yaml` được gắn Epic 20 để follow-through. (Các open action items hiện thuộc Epic 35/39/40 — xem bên dưới.)

## Action items

Không có fix-now item blocking. Một process note + một risk cần reconcile:
- **[defer/accept]** Per-crawler direct-emit decommission thực tế được thực hiện qua Epic 38 (Story 38.1/38.2) chứ không phải trong commit 20.x — ghi nhận là accepted deviation; không cần action.
- **[fix now]** `sprint-status.yaml` key derivation drift — `sprint_plan.py generate --dry-run` cho thấy parser derive ra story keys khác hoàn toàn với file hiện tại (33 `dropped_orphans` + 33 `new_entries`, ví dụ `20-1-...-x_scrape-x_actions_list-...` thay vì `20-1-multi-consumer-service-contract-...`). Reconcile `epics.md` story headings vs file keys trước khi bất kỳ ai chạy `generate` (non-dry-run) — nếu không sẽ phá file tracking. Owner: dev.

## Acceptance verdict

**accepted** (criteria: declared). Cả 2 story `done`, `pending_stories` rỗng, không có blocking finding mở. Acceptance criteria của spec (x_scrape envelope, x_actions_list 24-platform, extractRecords VN keys, canonical matrix, universal stream hook snake_case ThinEvent) đều có implementation evidence trên disk và commit attribution. Độ lệch duy nhất — phân bổ lại stream-hook work sang Epic 38 — là accepted deviation đã record.

## Open questions

- Story 20.3/20.4 (Nowing shadow-run parity ≥99% + legacy decommission) là external — cần xác nhận ở Nowing repo khi có điều kiện; không block verdict của repo này.
