---
epic: 26
date: 09-21-2026
verdict: accepted
criteria: declared
headless: false
---

# Retrospective — Epic 26

## Epic summary

Epic 26 (Legacy Decommission Final) hoàn thành intent: 26.1 tạo tag pre-decommission-2026-09-12 (8183c44b) + docs/decommission-plan-26.md (scope, rollback 48h, parity matrix); 26.2 (ddc16c7c) xóa sạch src/client/Scraper.js + src/scrapers/{twitter,facebook,threads,bluesky,mastodon}/ — net −9.399 dòng 137 files, archive 51 test legacy, package.json exports hybrid barrels, deprecation-plan.md→removed. DoD đạt: src/scrapers/social/ duy nhất chứa crawlers, index.js thin dispatcher qua descriptor registry, platforms.js route legacy names qua warn-proxies. 2 vòng review remediation (e01d0ac1, 0f9f4b8e — 15 findings, 13 patched). Chưa đạt theo chữ spec: (1) AC shadow-run 7 ngày field-level diff ≤1% không thực thi — parity là matrix khai báo + functional parity qua test suite (fallback criterion #2), 26.1 đàm phán lại AC nhưng không record deviation; (2) tag chưa push origin; (3) không story artifact 26.2; (4) unfollowx smoke test không evidence chạy riêng.

## Findings (aggregate views)

- **[spec-to-impl] (accept)** AC 26.1 shadow-run 7 ngày field-level diff ≤1%; thực tế chỉ parity matrix khai báo decommission-plan §3 + functional parity qua test suite — đúng fallback criterion #2 deprecation-plan nhưng không phải profiled parity. Story 26.1 renegotiate AC (parity report→matrix) nhưng epics.md không cập nhật deviation. Epic 20 retro xác nhận Nowing shadow-run 20.3/20.4 external, chưa confirm.
  - Source: `docs/decommission-plan-26.md §3; stories/26-1 (Intent+AC); epics.md:1572-1575; epic-20-retro-2026-09-21.md:48`
- **[spec-to-impl] (accept)** AC 'unfollowx CLI smoke test pass' (26.2) không evidence chạy riêng trong Epic 26; checklist §6 thiếu mục này. Bù: E2E live browser suite (6946fa91, 144d — dashboard/X/Facebook/Threads/admin JWT) + in-process admin CLI smoke (scripts/e2e-admin-cli.js 6b793c4b — trước Epic 26).
  - Source: `epics.md:1635-1636; decommission-plan-26.md §6; tests/e2e/admin-story-26.2-legacy-decommission.e2e.test.js; commit 6946fa91`
- **[spec-to-impl] (fix now)** Git tag pre-decommission-2026-09-12 tồn tại local (8183c44b) nhưng KHÔNG trên origin — git ls-remote --tags origin rỗng cho decommission; checklist §6 mục 1 yêu cầu 'đã tạo và push'. Rollback shield không khả dụng cho clone/CI khác.
  - Source: `git rev-parse pre-decommission-2026-09-12→8183c44b; git ls-remote --tags origin; decommission-plan-26.md §6`
- **[spec-to-impl (process)] (fix now)** Không story artifact Story 26.2 — stories/ có 42 file chỉ có 26-1-...md; 26.2 thực thi chỉ với commit + plan checklist. Điều kiện đầu deprecation-plan ('hybrid crawler done và pass tests' có chứng từ story) bị bỏ qua cho story cuối + quan trọng nhất.
  - Source: `ls stories/ (42 files chỉ 26-1); commits ddc16c7c..6946fa91`
- **[arch-delta] (accept)** Xóa vật lý đúng scope: 5 legacy dirs + Scraper.js biến mất (ls src/scrapers/twitter/ → No such file). adapters/ giữ lại — đúng điều kiện 'nếu đã consolidate'. DoD 'src/client/Scraper.js không tồn tại' thỏa theo file, nhưng class Scraper stub 266 dòng tái tạo trong src/client/index.js dispatch qua scrape() — surface backward-compat (NFR16) thay vì xóa hẳn. Accepted deviation có test (scraper-stub.test.js 8/8).
  - Source: `commit ddc16c7c (526+/9925-); src/client/index.js:25-26; tests/client/scraper-stub.test.js`
- **[parity-regression] (fix now)** Semantic drift Scraper stub sau decommission: (1) getLikedTweets(username) (client/index.js:104) gọi action 'likes' — nhưng 'likes' trong TwitterCrawler là likers-OF-A-TWEET (requiredArgs tweetId, crawler.js:191-212); legacy trả TWEETS user đã like — không action tương đương trong 40+ actions, call fail/thất bại im lặng. (2) getListTweets(listId) (client/index.js:224) map vào 'list_members' — trả members thay vì tweets của list (pre-decommission client/api/lists.js:76). scrapeLikes wrapper dùng tweetId: opts.tweetId||username — hợp lệ URL input nhưng username thành tweetId rác. Hai case không nằm trong 15 findings review.
  - Source: `src/client/index.js:104-110,220-229; twitter/crawler.js:191-212; git show pre-decommission-2026-09-12:src/client/api/lists.js:70-90`
- **[duplication] (defer)** Không duplication mới: 18 file twitter/http di chuyển 1:1 (0 content diff) vào social/twitter/http. Nhưng platforms.js hand-roll 3 no-op Proxy reddit/medium/instagram (không warn vô dụng) song song factory createDeprecationProxy có warn trong deprecation-proxy.js — 2 Proxy idioms cùng pipeline registry, nên gộp.
  - Source: `src/scrapers/platforms.js:46-72; deprecation-proxy.js:35-70`
- **[god-class] (accept)** Epic 26 không tạo god-class mới: index.js 546→585 (+39 back-compat), client/index.js 24→266 (stub @deprecated có test). social/twitter/crawler.js 4.429d + social/facebook/crawler.js 3.660d là growth Epic 13 — nhưng giờ là nơi duy nhất chứa platform logic, mọi addition tương lai tiếp tục bơm vào 2 file này.
  - Source: `wc -l: crawler.js 4429, fb 3660, index.js 585, client/index.js 266`
- **[pattern-divergence (meta)] (fix now)** 7 commits Epic 26.2 (ddc16c7c..6946fa91) authored 'user.email <nich@nichxbt.com>' thay vì nirholas — git config user.name bị đặt 'user.email', vi phạm rule CLAUDE.md, attribution history sai.
  - Source: `git log --format ddc16c7c..6946fa91; commits 8183c44b,88fea49e (nirholas đúng) vs ddc16c7c,f8420440,e01d0ac1,0f9f4b8e,316f8988,681f13e4,6946fa91 (user.email sai)`

## Behavior verification

Verify trực tiếp: npx vitest run tests/client/scraper-stub.test.js → 8/8 pass (instantiation, cookies, envelope unwrapping, method surface). Rollback window 48h đóng (merge 2026-09-13, retro 2026-09-21) không revert — ổn định production. Working tree: 5 legacy dirs + Scraper.js không tồn tại; exports trỏ social barrels (sau fix 0f9f4b8e); deprecation-plan.md statuses 'removed'; warn-proxies hoạt động. E2E live browser suite tồn tại (6946fa91) nhưng không log chạy trong retro. Nowing 24h post-merge parity (AC 26.2) external không verify được.

## Previous-retro follow-through

Epic 26 nằm trong nhóm `optional` retrospective; không có prior action item nào của epic này cần follow-through (các action items mở thuộc Epic 20/25/35 đã được xử lý riêng).

## Action items

- Sửa semantic drift src/client/index.js stub: getLikedTweets(username) gọi action 'likes' (likers-of-tweet, cần tweetId) thay vì tweets user đã like — bổ sung action 'liked_tweets' backend hoặc throw explicit unsupported; getListTweets(listId) map 'list_members' trả members thay list timeline — làm rõ unsupported hoặc implement. Thêm test chặn regression cả 2. *(owner: dev)*
- Push tag rollback lên origin: git push origin pre-decommission-2026-09-12 — checklist §6 yêu cầu 'đã tạo và push', hiện chỉ local. *(owner: dev)*
- Tạo story artifact Story 26.2 (stories/26-2-final-legacy-removal.md) hoặc ghi nhận chính thức sprint-status 26.2 chạy không-có-story-file, kèm record accepted-deviation shadow-run 7 ngày → functional parity fallback. *(owner: dev)*
- Sửa git author config: git config user.name nirholas (hiện 'user.email') để commit tương lai đúng attribution CLAUDE.md. *(owner: dev)*

## Pending / gated

- Không có.

## Acceptance verdict

**accepted** (criteria: declared).

## Open questions

- Nowing repo đã chạy parity window 24h sau merge 26.2 chưa (AC 26.2, external 20.3/20.4)?
- Có consumer npm bên ngoài phụ thuộc Scraper.getLikedTweets/getListTweets ngữ nghĩa legacy không — throw explicit hay implement backend action?
