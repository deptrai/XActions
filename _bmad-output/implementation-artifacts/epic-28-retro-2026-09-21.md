---
epic: 28
date: 09-21-2026
verdict: accepted
criteria: declared
headless: false
---

# Retrospective — Epic 28

## Epic summary

Epic 28 hoàn thành đúng phạm vi: 3 story done, 7 commit feat/fix + 2 sprint. SchemaDriftGuard score deterministic (cap missingOptional 20), phân loại complete/degraded/corrupted, throw DEGRADED_DATA + RETRY_WITH_DIFFERENT_ACCOUNT, wire qua AbstractCrawler.validateItem/filterValidItems trên 16/18 crawler. SelectorCanary node-cron gated ENABLE_SELECTOR_CANARY, alert AlertDispatcher (dedup 1h), platformDrift qua AdaptiveRateGovernor.getStatus() + badge dashboard. AutoSelectorFallback CLI 'xactions tools suggest-selector', heuristic data-testid(0.95)→aria(0.80)→id(0.75)→semantic(0.60), từ chối hash-class. 68/68 tests pass (4 files, 3.06s) không mock.

## Findings (aggregate views)

- **[scope_vs_spec] (accept)** 28.1: SchemaDriftGuard khớp AC — score formula, 3 tầng classification, ErrorTypes.DEGRADED_DATA + SuggestedActions.RETRY_WITH_DIFFERENT_ACCOUNT, zero dep mới. 2 lệch trong spec frozen: (1) cap min(20,5×missingOptional), (2) 'complete' yêu cầu missingOptional=0 thay '≥95' như epics.md — có lý do chống false-corrupted nhưng epics.md chưa đồng bộ.
  - Source: `src/core/schema-drift-guard.js:180-198; error-envelope.js:21,37; spec-28-1; epics.md:1735-1741`
- **[scope_vs_spec] (accept)** 28.2: epics.md 'Bull repeatable job hoặc setInterval' nhưng impl dùng node-cron (jobQueue.js chỉ nhận named processors). Signals successRate/usedFallback/driftDetected/lastWorkingSelector, ngưỡng <0.8 hai lần liên tiếp, badge dashboard đều đúng AC. Cron configurable SELECTOR_CANARY_CRON, gate ENABLE_SELECTOR_CANARY api/server.js.
  - Source: `src/services/selector-canary.js:327-345; api/server.js:785; adaptive-governor.js:678-679; admin.html:949-954,2025-2039`
- **[scope_vs_spec] (accept)** 28.3: CLI đúng 'xactions tools suggest-selector --platform --url --field' (cli/index.js:135). Ưu tiên data-testid→role/aria-*→id→semantic đúng spec; hash-only class từ chối cả compound (div.css-1a2b3c). Thang id=0.75 không trong epics.md (spec thêm bước 2b) — lệch nhỏ đã chốt.
  - Source: `src/cli/commands/tools.js:17-40; auto-selector-fallback.js:298-385; commit 0af965da`
- **[wiring_coverage] (fix now)** validateItem/filterValidItems wire vào 16 crawler (twitter,facebook,youtube,zalo,bluesky,mastodon,threads,instagram,medium,reddit,tiktok,linkedin,vietnamworks,topcv,batdongsan,chotot,shopee,tiktok-shop,masothue,automotive). Thiếu: identity/github + identity/gravatar (Epic 41, sau Epic 28) không gọi filterValidItems/validateItem — item đi thẳng ra caller.
  - Source: `src/scrapers/identity/github/crawler.js; identity/gravatar/crawler.js (no filterValidItems/validateItem); base-crawler.js:534-592`
- **[wiring_coverage] (defer)** Deferred-work 28.1 mục 3 PARTIALLY RESOLVED: dataQuality metadata test round-trip qua JsonStore nhưng quyết định Prisma persistence (column riêng vs fold vào metadata) vẫn treo — downstream SQL consumer không thấy dataQuality trừ khi nằm trong metadata.
  - Source: `deferred-work.md (spec-28-1 entry 3)`
- **[tests_verification] (accept)** 68/68 test pass 4 suite (schema-drift-guard, base-crawler-drift, selector-canary, auto-selector-fallback) — real impl, DI harness, không vi.mock, 3.06s. Review hiệu quả: 28.1 cần 3 đợt patch (8cdf3fc8 wire 11 crawler + b4982b07 DOM fallback), 28.3 cần 2 đợt (page-close leak, per-call browserFactory, protocol validation).
  - Source: `vitest run 2026-09-21: 4 files 68 passed; commits 8cdf3fc8,1d9a714d,b4982b07,67c3621e,0af965da`
- **[spec_drift] (fix now)** epics.md Story 28.1/28.2 AC không còn khớp impl (score thiếu cap, 'complete ≥95' vs 'missingOptional=0', Bull/setInterval vs node-cron). sprint-status đúng nhưng epics.md stale — cùng lớp key-drift Epic 20 retro xử lý.
  - Source: `epics.md:1735-1743 vs spec-28-1/spec-28-2 frozen`
- **[ops_readiness] (fix now)** Canary mặc định OFF (ENABLE_SELECTOR_CANARY=true, trong .env.example). Config canary-targets.json seed 4 platform đúng epics.md, mỗi target expectedShape. Rủi ro: target facebook (facebook.com/Meta) dùng selector 'div[data-pagelet=ProfileActions]' fallback — trang Facebook thường login-wall khi unauthenticated, chuỗi có thể fail toàn bộ kể cả UI không drift; chưa có bằng chứng probe thật.
  - Source: `config/canary-targets.json (facebook); .env.example (+7, commit 19f3ae09)`

## Behavior verification

Dữ liệu hỏng không lọt store âm thầm: item thiếu required/sai kiểu throw DEGRADED_DATA (fail-fast validateItem) hoặc filterValidItems drop+warn; degraded vẫn lưu kèm dataQuality. Drift DOM phát hiện định kỳ canary (khi bật gate), alert chỉ fire sau 2 lần liên tiếp <0.8 (dedup 1h), phơi qua governor.getStatus()/status-api + badge admin. Khi drift xác nhận, dev dùng suggest-selector lấy candidate confidenceScore ưu tiên thuộc tính bền, không đề xuất hash-class.

## Previous-retro follow-through

Epic 28 nằm trong nhóm `optional` retrospective; không có prior action item nào của epic này cần follow-through (các action items mở thuộc Epic 20/25/35 đã được xử lý riêng).

## Action items

- Đồng bộ epics.md Stories 28.1/28.2 AC text với spec frozen (cap min(20,5×missingOptional), complete=0 missingOptional, scheduler node-cron) — chấm dứt planning-artifact drift cùng lớp Epic 20 key-drift. *(owner: dev)*
- Chốt quyết định Prisma persistence cho item.dataQuality (column riêng vs fold vào metadata) trong deferred-work.md PARTIALLY RESOLVED của spec-28-1. *(owner: dev)*
- Wire filterValidItems vào GitHubCrawler + GravatarCrawler (Epic 41) để mọi crawler qua contract validation thống nhất. *(owner: dev)*
- Chạy 'xactions canary probe' 1 lần trên target thật (đặc biệt facebook.com/Meta) xác nhận chuỗi selector còn sống khi unauthenticated; nếu login-wall chặn thay target public khác (vd m.facebook.com) trong canary-targets.json. *(owner: dev)*

## Pending / gated

- Không có.

## Acceptance verdict

**accepted** (criteria: declared).

## Open questions

- Không có.
