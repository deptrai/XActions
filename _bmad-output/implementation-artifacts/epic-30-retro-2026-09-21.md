---
epic: 30
date: 09-21-2026
verdict: accepted-with-open-items
criteria: profiled
headless: false
---

# Retrospective — Epic 30

## Epic summary

Epic 30 giao đúng 2 stories (30-1 UniversalActionDispatcher, 30-2 ContentTransformer), cả hai đều có commit thật, spec done, 33/33 tests pass khi chạy lại (vitest run tests/scrapers/social: 1049 passed). Code tồn tại trên disk và được tích hợp đầy đủ: dispatcher.js (354 dòng, Promise.allSettled, thread-chaining post→reply, suggestedAction per failure), content-transformer.js (355 dòng, PLATFORM_LIMITS 280/300/500/500 khớp spec, media limits 4/4/4/10), scrape() route platform='all'/array tại src/scrapers/index.js:278-281, MCP tools x_publish_all/x_like_all/x_follow_all có schema+handler (server.js:1509/1541/1571, handlers 3468-3501), autoThread passthrough hoạt động. Caveats chính: (1) Threads write actions chỉ là dryRun-stub — live sẽ throw XACT_5001 'requires official Meta Threads API token' nên tuyên bố 'executes on X, Bluesky, Mastodon, and Threads' chỉ thật trên 3/4 nền tảng; (2) epic AC media nói 'video duration, file size, aspect ratio' nhưng transformer chỉ xử lý image-count (story 30.2 đã thu hẹp có chủ đích); (3) CLI surface 'post --sync-all' trong user story chưa có trong src/cli/index.js — chỉ có MCP + library surface; (4) không có review/test artifacts cho Epic 30 trong _bmad-output/review-artifacts và test-artifacts (khác các epic 13.x), findings chỉ nằm trong Review Triage Log của spec.

## Findings (aggregate views)

- **[spec-vs-delivery] (verified)** 2/2 stories delivered với 4 commits (3118cc0f 30.1, dfdd7223 fix requiresAuth, cf17f10a 5 review patches, bd32da02 30.2). Spec 30-1 và 30-2 status=done, mọi task checkbox [x], Review Triage Log ghi nhận 5 patch (threads post/post_detail collision, XACT_4010 dryRun fallback, targetId mapping, syntax dup) — có vòng review thật, không rubber-stamp.
  - Source: `_bmad-output/implementation-artifacts/spec-30-1-universalactiondispatcher-cross-platform-write-actions.md; git log 3118cc0f/dfdd7223/cf17f10a/bd32da02`
- **[code-artifacts] (verified)** dispatcher.js + content-transformer.js tồn tại và khớp Code Map: resolveTargetPlatforms trả ['twitter','bluesky','mastodon','threads'], SUPPORTED_WRITE_ACTIONS đủ post/like/reply/retweet/repost/follow/unfollow, aggregation shape {success,results,errors,summary:{total,succeeded,failed}} khớp context doc, transformForAll + PLATFORM_LIMITS re-export qua src/scrapers/index.js:154-155.
  - Source: `src/scrapers/social/dispatcher.js; src/scrapers/social/content-transformer.js; src/scrapers/index.js:154-155,278-281`
- **[tests] (verified)** Chạy lại toàn bộ suite social: 91 files, 1049 passed. 33/33 test của Epic 30 pass gồm fault isolation, credential ENV fallback, dryRun all-platforms, thread 3 phần qua autoThread, MCP registrations. Không có mock — dryRun là code path thật của crawler.
  - Source: `vitest run tests/scrapers/social/ — 1049 passed | tests/scrapers/social/universal-dispatcher.test.js (21 tests) + content-transformer.test.js`
- **[live-behavior-honesty] (disclosed-stub)** Threads write handlers là dryRun-only: live path throw PlatformError XACT_5001 'Threads live posting requires official Meta Threads API token or browser-as-signer bridge' (501). Bluesky (com.atproto.repo.createRecord XRPC thật) và Mastodon (/api/v1/statuses REST thật) là implementation thật; Twitter dùng GraphQL CreateTweet từ Story 13.2.6. Như vậy 'post to all' live = 3 nền tảng thật + Threads báo lỗi có suggestedAction — fault isolation đảm bảo an toàn nhưng claim 'executes on Threads' chưa profiled được.
  - Source: `src/scrapers/social/threads/crawler.js — async post() throw XACT_5001; src/scrapers/social/bluesky/client.js; src/scrapers/social/mastodon/client.js`
- **[process-artifacts] (acceptable-deviation)** Không có review-artifacts hay ATDD checklist nào cho Epic 30 (khác epic 13.x có đầy đủ hunter/triage JSON). Epic AC quy định per-platform isolation tại src/scrapers/social/<platform>/actions.js nhưng không file actions.js nào tồn tại — dùng registerAction trong crawler thay thế (epic-30-context.md cho phép 'hoặc tích hợp qua registerAction', nên là deviation có chấp thuận trong context nhưng lệch chữ epics.md). sprint-status.yaml đã mark epic-30: done, retrospective: optional.
  - Source: `_bmad-output/review-artifacts/ (no 30-* files); src/scrapers/social/*/crawler.js registerAction; _bmad-output/implementation-artifacts/sprint-status.yaml:200-203`

## Behavior verification

Hiện tại: x_publish_all (MCP) hoặc dispatchAction({platform:'all', action:'post'}) transform nội dung theo PLATFORM_LIMITS (280/300/500/500), tự tách thread có bảo vệ URL/mention/hashtag, đánh số i/n, đăng tuần tự post→reply móc xích per platform; media chunk 4/4/4/10 ảnh; autoThread:false tắt transform; lỗi 1 platform không chặn platform khác (Promise.allSettled), mỗi lỗi kèm code + suggestedAction. Live thật: Twitter (GraphQL), Bluesky (XRPC), Mastodon (REST). Threads: chỉ dryRun, live throw XACT_5001. Chưa có CLI command --sync-all.

## Previous-retro follow-through

Epic 30 nằm trong nhóm `optional` retrospective; không có prior action item nào của epic này cần follow-through (các action items mở thuộc Epic 20/25/35 đã được xử lý riêng).

## Action items

- Thêm disclosure vào description của x_publish_all/x_like_all/x_follow_all trong src/mcp/server.js: 'Threads live write chưa hỗ trợ (chỉ dryRun — cần Meta Threads API token)'. Người gọi MCP hiện không biết Threads sẽ luôn lỗi live. *(owner: dev)*
- Quyết định CLI surface: hoặc implement cờ --sync-all cho lệnh post trong src/cli/index.js, hoặc sửa wording user story trong epics.md từ 'post --sync-all' thành MCP-only để spec khớp thực tế. *(owner: pm)*

## Pending / gated

- Threads live write actions — gated trên Meta Threads API token hoặc browser-as-signer bridge (đã disclosed trong handler XACT_5001)
- Media rules epic-level: video duration, file size, aspect ratio — story 30.2 đã chủ đích thu hẹp còn image-count; phần còn lại cần story riêng

## Acceptance verdict

**accepted-with-open-items** (criteria: profiled).

## Open questions

- 'Epic done' có được chấp nhận khi 1/4 nền tảng (Threads) chỉ dryRun không, hay cần re-open như residual story khi Meta API bridge sẵn sàng?
- Có cần persist Review Triage Log (hiện nằm trong spec .md) thành review-artifacts JSON chuẩn hóa cho Epic 30 để đồng bộ quy trình với các epic khác không?
