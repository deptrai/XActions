---
epic: 39
date: 09-21-2026
verdict: accepted-with-open-items
criteria: profiled
headless: false
---

# Retrospective — Epic 39

## Epic summary

Epic 39 (Rescoped) hoàn thành đúng biên độ sau rescope. Duplication review (commit 45435111, 2026-09-18) xác định ~70% scope gốc đã có trong Stories 28.2/28.3 (SelectorCanary 442 dòng, AutoSelectorFallback 682 dòng); phần net-new chỉ còn GitOps Patch Assistant. Story 39.2 (commit 5e0aa29a) giao đủ 5 deliverable của rescope: canary heal CLI, CanaryHealer orchestrator, SelectorSandbox validation, unified-diff LCS (không thêm dependency), Draft PR qua gh + .patch fallback. expectedShape đã thêm cho cả 4 platform trong canary-targets.json. Xác minh lại hôm nay: 17/17 tests pass, wiring CLI đúng (registerCanaryCommand trong src/cli/index.js), types đầy đủ trong types/core.d.ts, sprint-status đã đánh done. Có 3 deviation nhỏ đã được ghi nhận trong Residual Risks hoặc phát hiện thêm trong review này — không blocker.

## Findings (aggregate views)

- **[Rescope completeness — 5/5 deliverables hiện hữu trên disk] (done)** Toàn bộ scope rescope đã ship: (1) src/cli/commands/canary.js — xactions canary {status|probe|heal} với --preview/--output/--platform/--target/--json; (2) src/services/canary-healer.js — CanaryHealer orchestrate investigate→validate→patch→PR; (3) src/services/selector-sandbox.js — validate(expectedShape) với tagName/minChildren/childSelectors/text/attributes predicates; (4) src/utils/unified-diff.js — generateJsonUnifiedDiff LCS-based; (5) #createDraftPr via gh pr create --draft + #writePatchFile fallback + #createInvestigationIssue khi no-candidates. KHÔNG re-implement SelectorCanary/AutoSelectorFallback — healer import từ '../core/auto-selector-fallback.js' đúng như cam kết rescope.
  - Source: `commit 5e0aa29a --stat; src/cli/index.js (import + registerCanaryCommand); src/services/canary-healer.js; _bmad-output/implementation-artifacts/spec-39-1-2-gitops-selector-healing-rescope.md §1.2`
- **[Tests & verification — re-run xác thực] (done)** Re-run hôm nay: npx vitest run tests/services/canary-healer.test.js tests/services/selector-sandbox.test.js tests/utils/unified-diff.test.js → 3 files, 17/17 pass (960ms). Tests tuân rule no-mocks: real file I/O trên temp config, real jsdom DOM + local HTTP server cho sandbox, injected execFn/browserFactory harness (không vi.mock). Coverage đủ 7/7 scenarios của I/O matrix: INVALID_TARGET (2), NO_CANDIDATES, SANDBOX_FAIL, PREVIEW, PATCH_FILE, GH_CLI_MISSING.
  - Source: `vitest run output 2026-09-21; tests/services/canary-healer.test.js; tests/services/selector-sandbox.test.js`
- **[Behavior deviation — drift status không được plumb qua heal mặc định] (deviation-documented-unmitigated)** Spec I/O matrix MULTI_ZONE nói 'heal all drifted platforms sequentially' nhưng CLI xây jobs từ TOÀN BỘ targets trong config (Object.keys(config)), không lọc theo governor platformDrift — nghĩa là heal mặc định sẽ launch stealth browser probe x.com/facebook/youtube/threads cho mọi target kể cả khi không drift. Đây là superset đã được tự khai trong Residual Risks của spec-39-2 nhưng chưa có guard (cost: browser launches + risk false-heal trên target chưa drift).
  - Source: `src/cli/commands/canary.js (heal action jobs construction); spec-39-2 Residual Risks; spec-39-1-2 §3.4`
- **[Spec contradiction — expectedShape optional vs throw] (deviation-stricter-than-spec)** Rescope spec §4 nói 'expectedShape is optional — nếu absent, sandbox fallback element-exists check only' nhưng heal() throw Error khi target thiếu expectedShape ('missing expectedShape — cannot validate candidates'). Hiện không break vì cả 4 targets đều có expectedShape, nhưng bất kỳ target mới thêm vào config thiếu expectedShape sẽ làm heal fail cứng thay vì fallback. Lưu ý: hướng implementation NGHIÊM ngặt hơn spec — an toàn hơn về Invariant #4 (validation mandatory) nhưng trái chữ spec.
  - Source: `spec-39-1-2-gitops-selector-healing-rescope.md §4 vs src/services/canary-healer.js heal() (throw trước investigate)`
- **[PR creation path — rủi ro worktree thật chưa exercised live] (risk-accepted-needs-guard)** #createDraftPr thao tác trực tiếp trên working tree của user: git checkout -b canary-heal/... → write config → commit → push → gh pr create → checkout -. Nếu working tree dirty, uncommitted changes bị cuốn theo branch mới; nếu checkout - fail, user bị kẹt ở heal branch. Đường này chỉ test qua injected execFn failure paths, chưa từng chạy live (Residual Risks thừa nhận). Không vi phạm Invariant #4 (vẫn là PR review gate, không hot-patch) nhưng là hazard vận hành thật.
  - Source: `src/services/canary-healer.js #createDraftPr; spec-39-2 Residual Risks bullet 2`
- **[Story 39.1 không có spec riêng] (minor-traceability-gap)** 39-1 (expectedShape config expansion) không có story spec standalone — được gộp vào spec-39-1-2 rescope spec và config đã commit cùng 39.2 trong 5e0aa29a (git log --follow config/canary-targets.json chỉ thấy 2 commits: 19f3ae09 từ Story 28.2 và 5e0aa29a). Sprint-status đánh 39-1 done là hợp lệ vì deliverable (4 expectedShape fields) đã verify trên disk, nhưng traceability story-level mỏng.
  - Source: `git log --follow config/canary-targets.json; config/canary-targets.json (4 expectedShape blocks); _bmad-output/implementation-artifacts/ (chỉ có spec-39-1-2 và spec-39-2)`
- **[Cross-epic consistency — AD-44 & FR-106 đồng bộ] (done)** PRD FR-106 đã rescope đúng (prd.md:203 'CLI tool xactions canary heal orchestrates...'), architecture.md có AD-44, epics.md có Rescope Note 2026-09-18, sprint-status có action_item epic-39-rescope-gitops-healing-only với status done và ref đúng file. Governor platformDrift plumbing (setPlatformDrift, adaptive-governor.js:792-816) tồn tại và được canary status command đọc qua getStatus().platformDrift — nhất quán chuỗi Detection→Healing.
  - Source: `prd.md:203; docs/architecture.md AD-44; sprint-status.yaml action_items; src/core/adaptive-governor.js:792-816`

## Behavior verification

Epic thực thi theo đúng flow GitOps khai báo: manual-trigger-only (không có scheduler/cron gọi heal), sandbox validation bắt buộc trước khi vào patch, output luôn là Draft PR/.patch/Issue — không có code path nào ghi trực tiếp selector vào runtime config hoặc Redis (xác minh bằng đọc toàn bộ canary-healer.js:唯一的 write là #writePatchFile ra file output và #createDraftPr qua git commit trên heal branch). Tests re-run 17/17 pass với real DOM. Điểm chưa xác thực end-to-end: heal --preview/live PR creation (cần browser thật + gh auth), theo đúng những gì Residual Risks đã khai.

## Previous-retro follow-through

Epic 39 nằm trong nhóm `optional` retrospective; không có prior action item nào của epic này cần follow-through (các action items mở thuộc Epic 20/25/35 đã được xử lý riêng).

## Action items

- Plumb governor platformDrift vào `canary heal` mặc định: khi không truyền --platform/--target, chỉ heal targets đang drift (đọc getStatus().platformDrift) thay vì toàn bộ config targets — tránh launch browser probe hàng loạt lên các trang chưa drift; hoặc thêm --all flag yêu cầu xác nhận rõ ràng khi heal toàn config. *(owner: dev)*
- Thêm dirty-worktree guard vào #createDraftPr: chạy `git status --porcelain` trước `git checkout -b`, abort với hướng dẫn dùng --output nếu working tree không sạch — tránh cuốn uncommitted changes vào heal branch. *(owner: dev)*
- Giải quyết mâu thuẫn expectedShape: cập nhật spec-39-1-2 §4 thành 'mandatory' (giữ nguyên implementation, an toàn hơn) HOẶC implement fallback element-exists per spec. Chọn 1, không để hai tài liệu nói khác nhau. *(owner: pm)*

## Pending / gated

- Không có.

## Acceptance verdict

**accepted-with-open-items** (criteria: profiled).

## Open questions

- Không có.
