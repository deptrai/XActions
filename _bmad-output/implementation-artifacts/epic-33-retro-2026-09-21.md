---
epic: 33
date: 09-21-2026
verdict: accepted
criteria: profiled
headless: false
---

# Retrospective — Epic 33

## Epic summary

Epic 33 (Zalo OA + YouTube VN) đã bàn giao 2 story `done` với bằng chứng kiểm chứng trực tiếp: 132/132 test unit+dispatch pass và 14/14 test E2E pass khi chạy lại hôm nay (2026-09-21). Story 33.1 đạt đầy đủ AC (oa_posts, oa_followers, oa_detail, marketplace_products + alias marketplace_search khớp spec `zalo_marketplace_search`). Story 33.2 đạt AC 1-9 của story nhưng một AC cấp epic đã bị bỏ lặng: 'HTML fallback via yt-dlp hoặc invidious khi quota cạn' — scan toàn bộ src/scrapers/social/youtube/ không thấy invidious/yt-dlp/fallback; khi hết quota crawler chỉ trả error envelope/backoff. Do đó 33.2 = accepted-with-open-items. Hai story gated 33.3/33.4 ở đúng trạng thái backlog-blocked/ready-for-dev với activation conditions ghi rõ — deferred by design, không phải thất bại.

## Findings (aggregate views)

- **[spec_vs_delivery] (accepted-with-open-items)** 33.1 khớp spec epic đầy đủ: ZaloCrawler/ZaloClient kế thừa AbstractCrawler/AbstractApiClient, 4 actions, chuẩn hóa PostItem/ProfileItem (platform 'zalo', category 'social', id tiền tố zalo:), AccountPool với hibernate khi lỗi -216, validator nhận diện mã lỗi Zalo v3 (-216/-211/-221/-32), persist + ThinEvent tới stream:social:raw_posts. 33.2 khớp spec ở search/trending_vn/channel_videos/channel_detail/video_detail/video_comments với regionCode VN mặc định, CommentItem threading parent-child, commentsDisabled flag. Điểm lệch duy nhất: AC 'HTML fallback via yt-dlp/invidious khi API quota exhausted' trong epics.md dòng 1935 không có mặt trong BDD AC 1-9 của story 33.2 và không có trong code.
  - Source: `_bmad-output/planning-artifacts/epics.md:1908-1947; stories/33-1 & 33-2; scan src/scrapers/social/{zalo,youtube}/`
- **[commits] (accepted)** 5 commit liên quan: e0beb4e5 (33.1 — 2.661 dòng, 7 file module + 62 test unit + 6 E2E), d6ea5754 (33.2 — 3.265 dòng, 78 test), 2bec68f6 (sửa bug khai báo trùng createYouTubeClient/createZaloClient trong dispatcher — phát hiện và vá trong cùng ngày 08/09, cho thấy integration wobble), b591d28b (bổ sung youtube/zalo vào dashboard/platform.html — đóng khoảng trống UI), b66dcb9c (forward httpClient DI cho YouTubeClient). Không có commit fix-back nào sau 10/09 cho 2 module này — không phát hiện defect còn treo.
  - Source: `git log --all -- src/scrapers/social/{zalo,youtube} + git show --stat`
- **[tests] (accepted)** Chạy lại trực tiếp: tests/scrapers/social/zalo + youtube = 12 file / 132 test pass (4.77s); tests/e2e/zalo-oa-crawler.e2e.test.js + youtube-vn-crawler.e2e.test.js = 2 file / 14 test pass (3.12s), bao gồm cả luồng lỗi nghiệp vụ (-216 → AuthSessionExpiredError) và full pipeline persist + publish. Story 33.2 có 5 review findings đều đã đánh dấu [x] patched.
  - Source: `npx vitest run tests/scrapers/social/{zalo,youtube} tests/e2e/{zalo-oa,youtube-vn}-crawler.e2e.test.js`
- **[docs_config] (accepted-with-open-items)** docs/canonical-action-matrix.md liệt kê đủ action matrix cho youtube (search/trending_vn/channel_*/video_*/comments) và zalo (oa_posts/oa_followers/oa_detail/marketplace); docs/architecture.md nhắc Zalo OA + YouTube VN trong Vietnam Local Ecosystem. Hai khoảng trống vận hành: (1) docs/api-reference.md không có mục nào cho zalo/youtube; (2) .env.example không khai báo YOUTUBE_API_KEY và ZALO_OA_ACCESS_TOKEN dù cả hai story phụ thuộc env này — operator mới sẽ không biết cần cấu hình gì.
  - Source: `docs/canonical-action-matrix.md:148-167; docs/architecture.md:43-130; grep .env.example (0 match)`
- **[governance] (deferred-by-design)** sprint-status.yaml: epic-33=done, 33-1=done, 33-2=done, 33-3/33-4=ready-for-dev (gated:true, backlog-blocked, 'DO NOT start until activation conditions confirmed'). 33.3 yêu cầu research spike 2 tuần + nhu cầu Nowing cụ thể + legal/compliance approve; 33.4 yêu cầu 33.2 ổn định production ≥2 tuần + tối ưu quota — đúng thiết kế 'conditional' của epic (note dòng 1921: personal Zalo là future work). Review-artifacts không có file triage riêng cho 33.x nhưng findings đã được ghi và vá trực tiếp trong story file.
  - Source: `_bmad-output/implementation-artifacts/sprint-status.yaml:214-219; stories/33-3 & 33-4 frontmatter`

## Behavior verification

Khi chạy thật: scrape('zalo'|'zalo_oa'|'zalo_official_account', oa_posts|oa_followers|oa_detail|marketplace_products, ...) gọi Zalo OpenAPI v3 qua AccountPool token, chuẩn hóa PostItem/ProfileItem, persist PrismaStore và publish thin-event tới Redis stream:social:raw_posts; lỗi -216/-211/-221/-32 và WAF HTML được phân loại đúng error envelope (auth_expired/rate_limit/login_wall/bot_challenge). scrape('youtube'|'yt'|'youtube_vn', search|trending_vn|channel_videos|channel_detail|video_detail|video_comments, ...) gọi Data API v3 với regionCode VN mặc định, trả CommentItem threading đa tầng, commentsDisabled trả mảng rỗng thay vì crash; hết quota (quotaExceeded) hiện chỉ backoff/trả envelope — KHÔNG có fallback yt-dlp/invidious như spec epic. Dispatcher tích hợp chung, dashboard hiển thị cả 2 platform. Zalo cá nhân (33.3) và advanced YouTube (33.4) chưa có behavior — gated.

## Previous-retro follow-through

Epic 33 nằm trong nhóm `optional` retrospective; không có prior action item nào của epic này cần follow-through (các action items mở thuộc Epic 20/25/35 đã được xử lý riêng).

## Action items

- Thêm YOUTUBE_API_KEY và ZALO_OA_ACCESS_TOKEN vào .env.example kèm chú ý về quota (YouTube 10k units/ngày) — cả 2 crawler đọc env này nhưng file mẫu không có *(owner: dev (nirholas))*
- Quyết định chính thức số phận AC 'HTML fallback yt-dlp/invidious' của story 33.2: hoặc mở follow-up task hiện thực, hoặc ghi descope có chữ ký PM vào story file (hiện bị bỏ lặng khi nâng cấp story lên BDD) *(owner: PM + dev)*
- Bổ sung section zalo/youtube vào docs/api-reference.md (hiện chỉ có trong canonical-action-matrix) *(owner: dev/docs)*

## Pending / gated

- 33-3-zalo-personal-messaging-research-spike-conditional
- 33-4-youtube-vn-advanced-data-conditional

## Acceptance verdict

**accepted** (criteria: profiled).

## Open questions

- Không có.
