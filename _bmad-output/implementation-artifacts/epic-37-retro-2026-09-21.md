---
epic: 37
date: 09-21-2026
verdict: accepted
criteria: declared
headless: false
---

# Retrospective — Epic 37

## Epic summary

Epic 37 ship 1/1 story (37.1) sau RESCOPE lớn: cơ chế engineTier hai tầng bị hủy vì trùng DESCRIPTORS+AbstractApiClient (consensus 3-agent, spec-37-1). Đã giao: đăng ký adapter got-jsdom, chèn _metadata.engineUsed/durationMs vào AbstractCrawler.start(), tài liệu AD-43. Chỉ tiêu 85% RAM + <800ms CHƯA BAO GIỜ đo (không benchmark artifact). DEFECT: engineUsed báo SAI cho các platform lightweight vì AbstractApiClient không có trường requiresBrowser — mọi HTTP client thật (MaSoThue, Batdongsan...) rơi nhánh 'browser'. Tests pass vì test client tự gán tay field không tồn tại trong base class.

## Findings (aggregate views)

- **[scope_vs_delivery] (accept)** Scope tuyên bố: engineTier tĩnh 2 tầng (Tier0 got-jsdom, Tier1 CDP/Puppeteer), 85% RAM, <800ms, engineUsed+durationMs. Thực tế: rescope bỏ routing layer — AD-43 ghi DESCRIPTORS+AbstractApiClient đã routing HTTP-first sẵn; chỉ còn telemetry+registry fix. Rescope hợp lý nhưng con số RAM/latency không có bằng chứng đo trong _bmad-output.
  - Source: `epics.md L2242-2261; spec-37-1 mục 1-3`
- **[code_correctness] (fix now)** BUG FIX-NOW: engineUsed tính bằng baseClient?.requiresBrowser===false?'http':'browser' (base-crawler.js L783-786) nhưng AbstractApiClient KHÔNG khai báo requiresBrowser (0 match trong base-client.js). 26 subclass thật không set → undefined===false=false → engineUsed='browser' SAI. Trường chỉ có ở tầng ADAPTER (adapters/base,http,got-jsdom,puppeteer,playwright) — khác layer với this.client. Metadata báo ngược engine cho đúng nhóm platform epic nhắm tới.
  - Source: `src/core/base-crawler.js L775-790; src/core/base-client.js fields L66-205 constructor L208-230`
- **[tests] (fix now)** engine-metadata.test.js 4/4 PASS (got-jsdom registered + http/browser/array). crawler-engagement 13/13 PASS sau chỉnh assertion. NHƯNG cả 2 test client TỰ GÁN this.requiresBrowser=false/true — contract không tồn tại trong AbstractApiClient. Tests mask bug: không test nào dùng subclass thật không gán tay. Verification gap kinh điển: test xanh nhưng production sai.
  - Source: `tests/scrapers/engine-metadata.test.js L27-33,54-60; vitest run 4/4 pass 2026-09-21`
- **[integration_consumption] (defer)** Không consumer nào của _metadata/engineUsed trong api/, src/cli/, src/mcp/ (0 match) — telemetry write-only, trái scope 'Phản hồi minh bạch metadata trong response'. _metadata không được strip trước serialize → rò vào mọi payload JSON. durationMs trùng biến cùng tên đã tính trong start().
  - Source: `0 match _metadata/engineUsed trong api/ src/cli/ src/mcp/; sprint-status.yaml L243-245`
- **[commits] (accept)** 2 commit cùng ngày 2026-09-18: b2a648e1 (telemetry+registry+AD-43+spec+sprint, 209d) và 277c4de3 (đổi 19 assertion toEqual→toMatchObject vì _metadata lệch shape kết quả — bằng chứng telemetry đổi contract công khai của scrape() ngay khi ship).
  - Source: `git show --stat b2a648e1; git show --stat 277c4de3`

## Behavior verification

Routing thật đúng (DESCRIPTORS+AbstractApiClient HTTP-first — AD-43 đúng). Bất thường: mọi kết quả scrape qua AbstractCrawler.start() mang _metadata nhưng engineUsed ĐẢO NGƯỢC so với thực tế cho toàn bộ 26 HTTP client thật (báo 'browser' khi thực tế 'http' qua got/undici); chỉ Puppeteer/CDP thật ngẫu nhiên báo đúng. Không crash, không ảnh hưởng data — chỉ sai observability.

## Previous-retro follow-through

Epic 37 nằm trong nhóm `optional` retrospective; không có prior action item nào của epic này cần follow-through (các action items mở thuộc Epic 20/25/35 đã được xử lý riêng).

## Action items

- Thêm trường requiresBrowser=false (default) vào AbstractApiClient (base-client.js, lưu từ options.requiresBrowser??false trong constructor) — HTTP client HTTP-first mặc định; Puppeteer crawler client override true. Sửa JSDoc @param. Sau đó engineUsed cho MaSoThue/Batdongsan/Chotot báo 'http' đúng. *(owner: dev)*
- Bổ sung test engine-metadata.test.js dùng AbstractApiClient subclass THẬT không gán tay requiresBrowser (vd MaSoThueClient) assert engineUsed==='http' — khóa contract chống tái phát. *(owner: dev)*
- Sửa engineUsed guard fail-safe: nếu client là AbstractApiClient (instanceof) → 'http', ngược lại 'browser', thay vì so sánh flag có thể undefined — phòng thủ khi field chưa thêm. *(owner: dev)*

## Pending / gated

- Không có.

## Acceptance verdict

**accepted** (criteria: declared).

## Open questions

- _metadata có chủ đích surface qua REST API (scope 'trong response') hay chỉ observability nội bộ/CLI? Hiện đi theo mọi payload nhưng không ai đọc — quyết định surface hay strip.
- Chỉ tiêu 85% RAM/<800ms: chấp nhận bỏ vì rescope (routing vốn HTTP-first) hay cần 1 benchmark session nhỏ (tier-0 vs tier-1) để đóng epic bằng số thật?
