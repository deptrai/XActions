---
epic: 29
date: 09-21-2026
verdict: accepted
criteria: profiled
headless: false
---

# Retrospective — Epic 29

## Epic summary

Epic 29 hoàn thành trong 1 ngày (14:07–21:36, 2026-09-16) qua 4 commit: 29.1 adapters (357ef829 + 19 patch review 0e22a72b), 29.2+29.3 (4b2f8a5e + patch 19fade1e). Kiểm chứng trực tiếp: STREAM_TYPES có 6 loại kể cả jetstream/mastodon_sse/cdc; adapters publish qua RedisStreamPublisher tới stream:social:raw_posts; dispatcher ký HMAC-SHA256 X-XActions-Signature, retry 1s/2s/4s rồi DLQ, admin routes mounted tại /api/admin/webhooks; replay API + MCP x_stream_replay trả events theo XRANGE với trim warning và X-XActions-Replay:true khi deliver webhook. Vitest: 12 files, 140 tests pass. Điểm trừ: signature chỉ ký body (không ký timestamp — đúng pattern lỗi repo đã fix ở commit 196de448 cho inbound webhooks); dispatcher tắt mặc định và ENABLE_OUTBOUND_WEBHOOK_DISPATCHER thiếu trong .env.example; docs/streaming.md + docs/streams.md vẫn mô tả polling-only 3 stream types.

## Findings (aggregate views)

- **[spec-coverage] (AC đạt; epics.md AC text cũ nên hiệu chỉnh (không ảnh hưởng code))** AC 29.1: đủ 4 file adapters (base-adapter/jetstream/mastodon-sse/cdc), STREAM_TYPES = ['tweet','follower','mention','jetstream','mastodon_sse','cdc'], publish qua RedisStreamPublisher, cursor persist + backoff. AC 29.2: dispatcher + subscription store + routes /api/admin/webhooks (subscriptions, delivery-logs, dlq, metrics, lag) mounted tại api/server.js:339. AC 29.3: getStreamReplay, GET /api/streams/:id/replay, MCP x_stream_replay (đăng ký + handler tại src/mcp/server.js:1114,5486), deliver=webhook hỗ trợ. Lệch duy nhất: epics.md ghi Jetstream host wss://bsky.network/xrpc/... nhưng code dùng wss://jetstream{1,2}.{us-east,us-west}.bsky.network/subscribe — code đúng với protocol Jetstream thật, story spec đã sửa.
  - Source: `src/streaming/streamManager.js:37; src/streaming/adapters/*; api/server.js:339; src/mcp/server.js:1114; epics.md:1776`
- **[process-commits] (Quy trình tốt; deferred được ghi nhận minh bạch)** Delivery flow chuẩn BMAD: spec trước (spec-29-1/2/3 với I/O matrix đầy đủ), commit feature, review loop riêng cho 29.1 (1 iteration, 19 patch: data integrity _emitEvent normalize, lifecycle refreshFromRedis, CDC pause cursor, reconnect fix), 29.2/29.3 patch in-band. Sprint-status.yaml: epic-29 done, cả 3 story done. Deferred-work.md ghi nhận 4 mục deferred có evidence rõ ràng.
  - Source: `git 357ef829, 0e22a72b, 4b2f8a5e, 19fade1e; sprint-status.yaml; deferred-work.md:162-181`
- **[tests-verification] (Coverage tốt; 2-3 verification gaps chưa đóng)** 140/140 tests pass (12 files): adapters 26, streamManager-adapters 4, stream-replay 28 (gồm REST 400/404/trim warning/webhook delivery với real HTTP server), webhook-dispatcher 32 (HMAC, tamper reject, retry 4-attempts, 404 thẳng DLQ, admin 401/403, secret redaction). Gaps còn mở theo triage log: consumer-loop XREADGROUP start() không có integration test (deferred — cần Redis fixture); webhook delivery success path trên REST replay route + multi-batch pagination filtered streams (VG entries) chưa thấy commit nào xác nhận đã thêm test.
  - Source: `vitest run tests/streaming (2026-09-21); spec-29-2/29-3 Review Triage Log; deferred-work.md:170-172`
- **[security-correctness] (Fix-now: timestamp binding cho outbound signature)** LỖ HỔNG: createSignature() ký HMAC-SHA256 trên body một mình, không có X-XActions-Timestamp và không ký timestamp — ai bắt được 1 delivery có thể replay vĩnh viễn. Repo đã fix đúng lỗi này cho notification webhooks ở commit 196de448 (ký <timestamp>.<body> theo chuẩn Stripe/GitHub). Đã fix trong epic: secret redaction API responses, NOGROUP error backoff, 429/408 retryable, skipDlq. Deferred có lý do: consumer-loop HOL blocking khi retry backoff (chặn cả batch), replay auth parity với GET streams khác.
  - Source: `src/streaming/outbound-webhook-dispatcher.js:36-38,585-599; commit 196de448; spec-29-2 triage; deferred-work.md:166-168,178-180`
- **[ops-docs] (Fix-now: thêm env var + cập nhật docs)** Dispatcher tắt mặc định (api/server.js:790 gate ENABLE_OUTBOUND_WEBHOOK_DISPATCHER === 'true') và biến này KHÔNG có trong .env.example — review triage đã flag là medium nhưng chưa thấy patch áp. docs/streaming.md + docs/streams.md (402 dòng) vẫn chỉ mô tả 3 polling stream types + Bull/Socket.IO, không nhắc adapters, outbound webhooks, replay API hay x_stream_replay. Graceful shutdown có gọi dispatcher.stop().
  - Source: `api/server.js:790; .env.example (grep ENABLE_OUTBOUND: 0 match); docs/streaming.md; docs/streams.md`

## Behavior verification

Hiện tại: streamManager hỗ trợ 6 stream types — 3 polling (tweet/follower/mention qua Bull) và 3 push adapters (Jetstream WebSocket, Mastodon SSE, CDC Redis XREAD BLOCK) publish ThinEvent vào stream:social:raw_posts với cursor persist + exponential backoff 1s→30s. OutboundWebhookDispatcher (tắt mặc định, bật bằng ENABLE_OUTBOUND_WEBHOOK_DISPATCHER=true) đọc stream qua consumer group, match subscriptions theo platform filter, POST ký X-XActions-Signature (HMAC-SHA256 body), retry 1s/2s/4s, 4xx thẳng DLQ xactions:webhook:dlq, metrics per-subscription. Replay: GET /api/streams/:id/replay?since=ISO|cursor=...&limit (max 1000) + MCP x_stream_replay trả events XRANGE oldest-first với nextCursor/hasMore, warning khi range bị trim, và ?deliver=webhook gửi qua dispatcher kèm X-XActions-Replay:true.

## Previous-retro follow-through

Epic 29 nằm trong nhóm `optional` retrospective; không có prior action item nào của epic này cần follow-through (các action items mở thuộc Epic 20/25/35 đã được xử lý riêng).

## Action items

- Ký timestamp vào HMAC outbound webhook: đổi createSignature thành HMAC(<timestamp>.<body>) theo pattern commit 196de448, thêm header X-XActions-Timestamp, xác minh freshness khi verify — tránh replay attack trên captured deliveries *(owner: nich (dev))*
- Thêm ENABLE_OUTBOUND_WEBHOOK_DISPATCHER=false + comment vào .env.example (triage medium chưa đóng) *(owner: nich (dev))*
- Cập nhật docs/streaming.md và docs/streams.md: 6 stream types, adapters push-based, outbound webhook API + signature scheme, replay endpoint + MCP x_stream_replay *(owner: nich (docs))*
- Hiệu chỉnh epics.md AC 29.1 (host Jetstream) và đánh dấu trạng thái 3 VG entries của spec-29-3 (webhook success path test, metadata filtering test, multi-batch pagination) — applied hoặc chuyển deferred-work *(owner: nich (PM/dev))*

## Pending / gated

- Bật dispatcher ở production: cần operator quyết định set ENABLE_OUTBOUND_WEBHOOK_DISPATCHER=true sau khi fix HMAC timestamp binding
- Integration test XREADGROUP consumer-loop thực sự cần Redis fixture riêng (deferred-work.md:170-172)
- Live smoke test Jetstream/Mastodon SSE trên mạng thật — tests hiện dùng local mock, chưa verify connectivity tới jetstream{1,2}.bsky.network / mastodon.social

## Acceptance verdict

**accepted** (criteria: profiled).

## Open questions

- Replay auth parity: GET /api/streams/:id/replay dùng cùng auth cấp server với GET streams khác (deferred-work.md:178-180) — có cần tighten riêng cho replay data không?
- MCP x_stream_replay stream-ID existence validation (triage medium trong spec-29-3) — không có commit patch nào nhắc tên mục này; đã áp chưa?
- Consumer-loop HOL blocking (1 subscriber chậm chặn cả batch do await backoff inline) — chờ delivery worker ở epic sau hay fix sớm?
