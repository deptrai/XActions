---
epic: 31
date: 09-21-2026
verdict: rejected
criteria: declared
headless: false
---

# Retrospective — Epic 31

## Epic summary

Epic 31 (UniversalMediaPipeline, Story 31.1 done d71c289b 926d/8files): UniversalMediaPipeline src/scrapers/social/media-pipeline.js (417d) có đủ 6 adapter, schema MediaObject đúng spec, videoDownloader delegate, x_download_media đăng ký cả local-tools.js+server.js TOOLS, architecture.md cập nhật. 14/14 test pass. NHƯNG 3 lỗ hổng thật: (1) đường postUrl-only gọi scrape(platform,'post_detail') nhưng twitter/bluesky/mastodon/facebook KHÔNG có action 'post_detail' → actionNotAvailable → catch trống → payload {url} → trả rỗng success:true; (2) XACT_4001 cho URL invalid không được hiện thực (import PlatformError/SuggestedActions không dùng); (3) tham số quality không honor. Tests đều inject payload trực tiếp nên che lỗi tích hợp.

## Findings (aggregate views)

- **[spec-vs-delivered] (defer)** 7/8 AC có code: MediaObject schema đúng; 6 adapter đủ; HLS fallback parseMediaEntity (MP4 bitrate cao→.m3u8); carousel Threads carousel_media; Spaces audio; MCP postUrl+platform+quality; videoDownloader delegate. Thiếu: Twitter voice posts (normalize-media không xử lý 'voice'/'audio' trong extended_entities); selectBestVariant không tồn tại (logic trong normalize-media); DASH chỉ trong comment.
  - Source: `epics.md L1849-1869; src/scrapers/social/media-pipeline.js; twitter/normalize-media.js`
- **[runtime-integration] (fix now)** AC 'x_download_media hỗ trợ postUrl trả MediaObject[]' chỉ đúng khi caller tự cấp payload. Đường postUrl-only: extractMedia gọi scrape(platform,'post_detail',{url}) — post_detail chỉ có ở threads/tiktok/instagram/reddit/medium; twitter/bluesky/mastodon/facebook không map → actionNotAvailable throw → catch trống → payload={url} → adapter trả [] → MCP trả success:true count:0. User gọi link x.com nhận kết quả rỗng không báo lỗi.
  - Source: `media-pipeline.js extractMedia try/catch; scrapers/social/{twitter,bluesky,mastodon,facebook}/descriptor.js (không có post_detail); mcp/local-tools.js`
- **[error-handling] (fix now)** I/O matrix frozen spec yêu cầu postUrl='invalid-url' → throw XACT_4001 + suggestedAction. Thực tế: media-pipeline.js import PlatformError/ErrorTypes/SuggestedActions nhưng không dùng (0 refs), không có XACT_4001 trong pipeline/error-envelope. URL sai format chỉ fallback detectPlatformFromUrl về 'twitter'. local-tools chỉ chặn thiếu postUrl, trả error string không theo envelope.
  - Source: `spec-31-1 I/O matrix; media-pipeline.js unused imports; src/core/error-envelope.js`
- **[test-evidence] (fix now)** vitest media-pipeline.test.js 14/14 pass (2.5s), coverage tốt adapter/dispatch/toolMap/TOOLS. NHƯNG 100% test inject post payload trực tiếp — không test postUrl-only (đường đang hỏng), không test invalid-URL envelope, không test quality. Test pass không phản ánh hành vi MCP tool thật.
  - Source: `tests/scrapers/social/media-pipeline.test.js (274d, toàn cases post/rawData); vitest run 2026-09-21`
- **[status-and-docs] (accept)** sprint-status.yaml L206 '31-1-...: done' nhất quán commit. 1 commit duy nhất d71c289b (2026-09-17). architecture.md nhắc UniversalMediaPipeline+x_download_media. quality param trong MCP inputSchema nhưng extractMedia/downloadMedia bỏ qua.
  - Source: `sprint-status.yaml L206; git log d71c289b; docs/architecture.md; src/mcp/server.js TOOLS`

## Behavior verification

Caller truyền payload (post/rawData) — pipeline đúng AC: chọn MP4 bitrate cao nhất, HLS fallback, trả MediaObject[] chuẩn. Caller chỉ có postUrl (kịch bản MCP thật): twitter/bluesky/mastodon/facebook trả success:true media rỗng do descriptor thiếu post_detail + catch trống nuốt lỗi; chỉ threads/tiktok (+instagram/reddit/medium ngoài scope) trích được URL-only.

## Previous-retro follow-through

Epic 31 nằm trong nhóm `optional` retrospective; không có prior action item nào của epic này cần follow-through (các action items mở thuộc Epic 20/25/35 đã được xử lý riêng).

## Action items

- Thêm mapping 'post_detail' vào descriptor twitter/bluesky/mastodon/facebook (map về thread/timeline tương đương) hoặc đổi extractMedia sang action đúng từng platform — để x_download_media hoạt động URL-only đủ 6 platform theo AC *(owner: dev)*
- Hiện thực envelope XACT_4001 (PlatformError+SuggestedActions InvalidArgs) cho postUrl invalid trong extractMedia/local-tools theo I/O matrix frozen; bỏ catch trống để lỗi dispatch không bị nuốt thành rỗng *(owner: dev)*
- Bổ sung test: postUrl-only qua scrape dispatch (không inject post), invalid-URL trả XACT_4001 envelope, hành vi quality — chặn regression đường MCP thật *(owner: dev)*

## Pending / gated

- Không có.

## Acceptance verdict

**rejected** (criteria: declared).

## Open questions

- Twitter voice posts (AC 'audio extraction (voice posts, Spaces)') chưa adapter nào xử lý — tách story 31.2 riêng hay reopen 31.1?
- Tham số quality ('highest'|'lowest'|'all') nên honor thật (lọc variants theo bitrate) hay xóa khỏi MCP schema tránh hợp đồng ảo?
