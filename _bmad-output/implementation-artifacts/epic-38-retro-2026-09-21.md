---
epic: 38
date: 09-21-2026
verdict: accepted
criteria: declared
headless: false
---

# Retrospective — Epic 38

## Epic summary

Epic 38 (Crawler Lifecycle Unification & CloudEvents Standardization) được thực thi đúng: Story 38.1 (commit 3673757a) xóa sạch cờ __streamEmitted khỏi src/ (chỉ còn trong test như assertion tự-bảo-vệ), tập trung phát sự kiện vào AbstractCrawler.emitStreamBatch() với dedup session-scoped (_emittedItemIds) và kế thừa context (_currentContext); Story 38.2 (commit 1dc3c03e) chuẩn hóa CloudEvents v1.0 ngay tại tầng RedisStreamPublisher.formatPayload() (specversion/id/source/type/time/datacontenttype/data + idempotencyKey sha256 theo bucket giờ, dual-emit idempotencykey lowercase), giữ 100% flat fields cho consumer cũ. Vòng review dày đặc: 38.1 xử lý 29 findings (26 patch, 3 reject có lý do), 38.2 xử lý 5 findings. Kiểm chứng lại độc lập hôm nay: 4 suite của epic pass 54/54 trong 1.46s (XACTIONS_TEST_FAST_DELAYS=1); tests/core/ toàn phần 420 pass/15 skip, 2 file fail là Story 13.1 do thiếu binary Playwright chromium trên máy (env infra, không liên quan Epic 38). Vấn đề cốt lõi: epic tuyên bố giải quyết split-brain 'triệt để' và absorb phần decommission per-crawler emit của Story 20.2, nhưng chỉ phủ 7/9 platform trong AC của 20.2 — YouTubeVNCrawler và ZaloCrawler (đều extends AbstractCrawler) vẫn gọi this.publisher.publish() trực tiếp trong #persistPosts, bypass _emittedItemIds nên khi publisher được inject sẽ double-emit; đồng thời result.__streamCursor (AC 20.2) đã bị gỡ khỏi base khiến src/mcp/server.js:4166 luôn trả cursor=null, và AC stream_delivery:'ok'|'failed' không tồn tại trong src/ dù sprint-status vẫn đánh 20-2 done.

## Findings (aggregate views)

- **[scope-conformance] (confirmed)** Scope liệt kê được bàn giao đủ: __streamEmitted = 0 occurrence trong src/ (tgrep chỉ còn thấy trong tests/core/base-crawler-lifecycle.test.js như assertion 'không được tồn tại'); emitStreamBatch hiện diện ở base-crawler.js (2 refs) và cả 7 crawler được liệt kê (threads 2, facebook/instagram/tiktok/twitter/reddit/medium mỗi cái 1); validation start() với dryRun, gate REDIS_STREAM_ENABLED, guard publishOk đều có trong code.
  - Source: `tgrep toàn repo; src/core/base-crawler.js; src/scrapers/social/{threads,facebook,instagram,tiktok,twitter,reddit,medium}/crawler.js; commits 3673757a, 1dc3c03e`
- **[scope-conformance] (gap-needs-fix)** Nghịch lý chữ ký scope: bullet 'Trong scope' của Epic 38 viết 'Xoá bỏ TẤT CẢ các lệnh gọi publisher.publish(...) trong các crawler con' nhưng Code Map của spec 38.1 chỉ enumerate 7 crawler — YouTube và Zalo không được liệt kê và vẫn giữ this.publisher.publish(item, this.scraperId) trực tiếp (youtube/crawler.js:282-286, zalo/crawler.js:771-775). Đây là khoảng hở giữa chữ 'tất cả' (prose) và danh sách file (enumerate) trong chính spec của epic.
  - Source: `_bmad-output/planning-artifacts/epics.md dòng 2230; _bmad-output/implementation-artifacts/spec-38-1...md (Code Map); src/scrapers/social/youtube/crawler.js:282-286; src/scrapers/social/zalo/crawler.js:771-775`
- **[verification-evidence] (confirmed)** Chạy lại độc lập 4 suite của epic: 4 test files, 54 tests pass, 1.46s với XACTIONS_TEST_FAST_DELAYS=1 (đúng cam kết <1.5s/<2s). Chạy rộng tests/core/ + tests/store/redis-stream-publisher.test.js: 420 passed / 15 skipped / 2 files failed — 2 failure là signer-pool.test.js và base-client-sign.test.js (Story 13.1) do thiếu executable Playwright chromium_headless_shell-1234 trên máy và 1 hook timeout 30s; các file này lần cuối được đụng bởi Story 13.1.2 (cf9a9d96), trước Epic 38 — không phải regression của epic.
  - Source: `vitest run 2026-09-21 tại cwd; git log tests/core/signer-pool.test.js`
- **[verification-evidence] (confirmed)** Chất lượng review loop cao: 38.1 ghi nhận 29 findings với 2 patch high quan trọng (context inheritance tránh mất workspaceId/targetId ở mid-flight emit; re-init _emittedItemIds mỗi start() và dọn trong finally chống re-entrancy; guard chỉ đánh dấu emitted khi publishOk=true). 3 reject có lý do rõ (unnamespaced IDs vô hại trong 1 run; __streamCursor tách về 20.2; zero-stream-test cho platform phụ vì đã có unit test nền). 38.2: 5 findings đều patch (prefix normalization cho entityId, MIME case-insensitive, assertion CloudEvents trực tiếp trên mapToThinEvent).
  - Source: `_bmad-output/implementation-artifacts/spec-38-1...md (Review Triage Log); spec-38-2...md (Review Triage Log)`
- **[cross-epic-reconciliation] (gap-needs-fix)** Epic 38 absorb phần 'decommission per-crawler direct emit' của Story 20.2 nhưng chỉ hoàn thành cho Facebook/Threads/Medium/Reddit/TikTok/Twitter/Instagram (7 platform). AC của 20.2 liệt kê rõ 'gỡ bỏ ThreadsCrawler, Medium, Reddit, YouTube, Zalo #emitCheckpointAndStream' — YouTube và Zalo bị bỏ sót: cả 2 là subclass của AbstractCrawler nên execute() sẽ phát lại batch ở completion-pass trong khi direct publish ở #persistPosts không đăng ký vào _emittedItemIds → split-brain gốc của epic còn tồn dư dạng latent (chỉ kích hoạt khi deps.publisher được inject; wiring mặc định qua actions-list.js tạo crawler không deps nên publisher=null, chưa double-emit trong runtime mặc định).
  - Source: `_bmad-output/planning-artifacts/epics.md:1237-1300 (Story 20.2 AC); src/scrapers/social/youtube/crawler.js:282-286; src/scrapers/social/zalo/crawler.js:771-775; src/scrapers/social/actions-list.js; epic-38-context.md`
- **[cross-epic-reconciliation] (gap-needs-fix)** Reject của 38.1 triage đối với finding '__streamCursor bị mất' (lý do: 'là concern MCP-envelope của Story 20.2, ngoài scope 38.1') tạo ra kẽ hở sở hữu: Story 20.2 đã đóng (sprint-status: 20-2 done), Epic 38 đã đóng — không story nào sở hữu việc cursor. Kết quả: src/mcp/server.js:4166 đọc result?.__streamCursor từ result của crawler mà không crawler nào set nữa → x_scrape luôn trả stream.cursor=null một cách im lặng, phá consumer contract của 20.2. Tương tự AC stream_delivery:'ok'|'failed' của 20.2 không tồn tại ở bất kỳ đâu trong src/ (tgrep 0 match) dù sprint-status vẫn đánh 20-2 done — drift trạng thái.
  - Source: `spec-38-1 Review Triage Log (reject __streamCursor); _bmad-output/implementation-artifacts/sprint-status.yaml:146 (20-2: done); src/mcp/server.js:4166; tgrep 'stream_delivery' = 0 match`
- **[consumer-contract-integrity] (confirmed)** CloudEvents v1.0 được đặt đúng tầng publisher (formatPayload) nên cả đường direct publish lẫn đường emitStreamBatch đều nhận envelope chuẩn; dual-emit đầy đủ (idempotencyKey + idempotencykey lowercase, snake_case + camelCase flat fields, benchmark_health/benchmark_alert) bảo toàn consumer cũ Nowing/ChainLens. computeIdempotencyKey chuẩn hóa prefix 'platform:' và fallback bucket — determinism được test asserting hai item cùng giờ ra cùng hash.
  - Source: `src/utils/redis-stream-publisher.js (computeIdempotencyKey, validateCloudEvent, formatPayload); tests/core/cloudevents-compliance.test.js; tests/store/redis-stream-publisher.test.js`
- **[process-hygiene] (confirmed)** Quy trình sạch: sprint-status.yaml cập nhật nguyên tử theo từng commit (3673757a: 38-1 ready-for-dev→done, 38-2 backlog→ready-for-dev; 1dc3c03e: epic-38 in-progress→done, 38-2→done); baseline_revision của 38.2 (3673757a...) trùng đúng commit 38.1 — chuỗi dependency 38.1 blocks 38.2 được tôn trọng; commit message theo convention, có Co-Authored-By; epic-38-context.md ghi rõ AD-41 và dependency Epic 38 unblocks Epic 36.
  - Source: `git show 3673757a / 1dc3c03e (sprint-status diff); spec headers baseline_revision; _bmad-output/implementation-artifacts/epic-38-context.md`

## Behavior verification

Tại runtime mặc định: mỗi item của 7 platform được enumerate (facebook, threads, instagram, tiktok, twitter, reddit, medium) phát đúng 1 lần qua emitStreamBatch với dedup theo id/storage_ref trong phạm vi session; dryRun (args/session/_currentDryRun) chặn phát sự kiện; REDIS_STREAM_ENABLED=false → skip không lỗi; publish fail → warn non-blocking và item KHÔNG bị đánh dấu emitted (cho phép retry lần sau); workspaceId/targetId kế thừa từ _currentContext của start() kể cả khi subclass gọi emitStreamBatch mid-flight thiếu context. Mọi sự kiện ra Redis đều mang envelope CloudEvents v1.0 + idempotencyKey sha256 bền định (bucket giờ UTC) + dual-emit flat fields. Ngoại lệ hành vi: YouTube/Zalo khi được inject publisher sẽ phát 2 lần (1 direct không dedup + 1 từ completion-pass); x_scrape luôn trả stream.cursor=null; stream_delivery không tồn tại.

## Previous-retro follow-through

Epic 38 nằm trong nhóm `optional` retrospective; không có prior action item nào của epic này cần follow-through (các action items mở thuộc Epic 20/25/35 đã được xử lý riêng).

## Action items

- Migrate #persistPosts (và các persist method tương tự) của YouTubeVNCrawler và ZaloCrawler sang this.emitStreamBatch(valid, { targetType, targetKey }) thay cho vòng lặp this.publisher.publish trực tiếp, khép kín split-brain theo đúng prose scope 'xoá bỏ TẤT CẢ publisher.publish trong crawler con' của Epic 38 và AC decommission của Story 20.2. *(owner: bmad-dev (follow-up patch gắn nhãn Epic 38/Story 38.1 fix-now))*
- Khôi phục __streamCursor (hoặc trường mới streamCursor) từ lastEventId của lần publish thành công cuối trong emitStreamBatch để src/mcp/server.js:4166 trả cursor thật; nếu quyết định bỏ contract này thì xóa dòng đọc cursor và cập nhật sprint-status ghi chú decision cho 20-2. *(owner: bmad-dev (cursor) + bmad-sm (cập nhật 20-2 status note))*
- Định đoạt AC stream_delivery:'ok'|'failed' của Story 20.2: hoặc implement flag vào envelope x_scrape, hoặc đánh dấu AC bị superseded bởi Epic 38 (CloudEvents + idempotencyKey thay thế) trong spec 20.2 để sprint-status 'done' không còn drift. *(owner: bmad-pm (decision) + bmad-sm (spec/status reconciliation))*

## Pending / gated

- Không có.

## Acceptance verdict

**accepted** (criteria: declared).

## Open questions

- Đường deps.publisher/eventPublisher của YouTube/Zalo có được inject ở production dispatcher nào ngoài actions-list.js không — nếu không, double-emit chỉ là latent risk của test/integration và có thể xếp ưu tiên thấp hơn.
- Consumer Nowing/ChainLens đã bắt đầu đọc idempotencyKey chưa, và có phụ thuộc vào stream.cursor/stream_delivery từ hợp đồng 20.2 cũ không (quyết định action 2/3 nên dựa trên xác nhận này).
