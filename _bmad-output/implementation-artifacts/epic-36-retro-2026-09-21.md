---
epic: 36
date: 09-21-2026
verdict: accepted
criteria: declared
headless: false
---

# Retrospective — Epic 36

## Epic summary

Epic 36 hoàn thành đúng scope: MCP tool x_social_find_profiles fan-out qua Universal Scrape Dispatcher, chuẩn hoá SĐT VN (single source src/utils/vn-phone.js), fault isolation tier-aware + circuit breaker single-probe per-account, error taxonomy, account health guard. Story 36.2 consolidate vào 36.1 nhưng implement đầy đủ (d7a293ef). 30/30 tests pass. Vượt scope: expose thêm API route, CLI, dashboard (6d5198a1, Epic 41 xây tiếp). 3 deferred findings từ code review đã resolve+xác minh.

## Findings (aggregate views)

- **[Scope delivery] (accept)** MCP tool đăng ký đủ schema (query,queryType,locale,platforms,timeoutMs,accountId,proxyUrl,context) server.js:3010, dispatch :3463, wrapper executeSocialFindProfilesTool :3937; engine osint-find-profiles.js fan-out qua scrape(platform,action,args) đúng spec.
  - Source: `src/mcp/server.js; src/mcp/osint-find-profiles.js; epics.md:2196-2219`
- **[Scope delivery] (accept)** VN phone normalize: MASKED_PHONE_RE test raw trước khi strip dấu chấm (fix '090...'), VN_PHONE_RE mở rộng 5[25689]; normalizeVnPhone('0521234567')→'0521234567' đúng.
  - Source: `src/utils/vn-phone.js; commit e577c65b`
- **[Fault isolation & resilience] (accept)** Circuit breaker single-probe half-open (flag probing), key platform:accountId, evictCircuits cap 200 — 3 deferred findings code review 36.1 đều RESOLVED + test tương ứng.
  - Source: `src/mcp/osint-find-profiles.js:96-158; deferred-work.md:183-198; commit e577c65b`
- **[Fault isolation & resilience] (accept)** Story 36.2 đầy đủ: PLATFORM_TIMEOUTS_MS tier0 4-5s/tier1 15s, classifyPlatformError (RATE_LIMITED/BOT_BLOCKED/AUTH_REQUIRED/PLATFORM_TIMEOUT), account health guard 'account_sick' khi isHibernating. Drift nhỏ: taxonomy ở platformStatus.error.category thay vì error.code như spec §1.2 (code giữ rawCode backwards-compat).
  - Source: `osint-find-profiles.js:425-475,565-570; spec-36-2; commit d7a293ef`
- **[Fault isolation & resilience] (accept)** Spec ghi Promise.allSettled() fan-out; impl dùng runPool giới hạn 4 platform (MAX_CONCURRENT_PLATFORMS) + allSettled-style — lệch chữ spec nhưng đúng tinh thần partial-failure, an toàn resource hơn (16 platform Puppeteer).
  - Source: `osint-find-profiles.js:553-565,577-583`
- **[Testing & evidence] (accept)** 30/30 tests pass khi chạy lại (2.94s), không mocks; phủ XACT_4001, detectQueryType, circuit open/single-probe/per-account, timeout taxonomy, all-fail vẫn success:true profiles rỗng.
  - Source: `tests/mcp/osint-find-profiles.test.js; vitest run 2026-09-21`
- **[Testing & evidence] (defer)** 2 e2e pilot: browser pilot (0d558c5a) + live network fan-out 10 platform (9bc77044 LinkedIn+VietnamWorks found). Cả 2 là .mjs manual, KHÔNG trong vitest/CI — có bằng chứng pilot nhưng chưa có regression-guard CI.
  - Source: `tests/e2e-osint-browser-pilot.mjs; tests/e2e-osint-deptrai-live.mjs; commits 0d558c5a,9bc77044`
- **[Integration surface] (accept)** Vượt scope có kiểm soát: POST /api/osint/find-profiles + GET /api/osint/platforms (authenticateToken), CLI 'xactions osint find-profiles', dashboard/osint.html — giúp Nowing/ChainLens consume ngoài MCP.
  - Source: `api/routes/osint.js; src/cli/commands/osint.js; dashboard/osint.html; commit 6d5198a1`
- **[Integration surface] (fix now)** dashboard/osint.html gọi fetch('/find-profiles') và '/platforms' KHÔNG kèm Authorization header trong khi route bắt authenticateToken → UI luôn 401 'No token provided' khi chạy thật — dashboard tê liệt ngay từ đầu.
  - Source: `dashboard/osint.html:160,230; api/routes/osint.js:14,45; api/middleware/auth.js:38-40`
- **[Ops & security] (fix now)** /api/osint không có dedicated limiter: chỉ dính global /api/ limiter 500 req/15min trong khi mỗi request fan-out tới 4 scrape đồng thời (nhiều Puppeteer) — cùng chi phí /api/graph,/api/crm vốn đã có heavyLimiter 10/15min. Route chưa có tier/subscription gating dù OSINT nhạy cảm+tốn kém.
  - Source: `api/server.js:162-199; osint-find-profiles.js MAX_CONCURRENT_PLATFORMS=4`
- **[Code quality & process] (fix now)** VN_PHONE_BROAD_RE (parseVnPhone cho healthcare/automotive) vẫn giữ 5[689] trong khi VN_PHONE_RE đã mở rộng 5[25689] — parseVnPhone('0521234567')→{phone:null} trong khi normalizeVnPhone OK; hai regex cùng file drift prefix class. Process: 9760dc03 mark epic done 11:02 TRƯỚC khi commit 36.2 (d7a293ef) land 11:37 — mark done trước merge.
  - Source: `src/utils/vn-phone.js:30 vs :26; git log 9760dc03,d7a293ef`

## Behavior verification

x_social_find_profiles (hoặc POST /api/osint/find-profiles, CLI) với query name/username/phone/email: auto-detect queryType (phone không phải SĐT VN degrade về name), normalize SĐT VN trước dispatch chotot/zalo/masothue, fan-out ≤4 platform deadline tier-aware (Tier0 4-5s, Tier1 15s) AbortController+withTimeout, guard account hibernating, skip circuit mở (single-probe half-open 60s, threshold 3, key platform:accountId, evict 200), classify RATE_LIMITED/BOT_BLOCKED/AUTH_REQUIRED/PLATFORM_TIMEOUT/SCRAPE_ERROR, normalize ProfileItem[] + identityClusters (41.2/41.3), luôn success:true + platformStatus per-platform kể cả all-fail.

## Previous-retro follow-through

Epic 36 nằm trong nhóm `optional` retrospective; không có prior action item nào của epic này cần follow-through (các action items mở thuộc Epic 20/25/35 đã được xử lý riêng).

## Action items

- Thêm Authorization header (Bearer) vào fetch trong dashboard/osint.html cho /find-profiles + /platforms — hiện luôn 401 vì route authenticateToken; xử lý 401 hiển thị prompt đăng nhập. *(owner: dev)*
- Mount heavyLimiter (10 req/15min) cho /api/osint trong api/server.js — fan-out OSINT tốn resource tương đương /api/graph,/api/crm; cân nhắc tier gating (pro/enterprise). *(owner: dev)*
- Đồng bộ prefix class trong src/utils/vn-phone.js: VN_PHONE_BROAD_RE đổi 5[689]→5[25689] để parseVnPhone nhận 052/059 như normalizeVnPhone; thêm test 052 qua parseVnPhone. *(owner: dev)*

## Pending / gated

- Không có.

## Acceptance verdict

**accepted** (criteria: declared).

## Open questions

- E2E pilot scripts (e2e-osint-browser-pilot.mjs, e2e-osint-deptrai-live.mjs) chưa trong vitest/CI — giữ manual hay wire vào suite?
- Spec 36.2 taxonomy ở error.code nhưng impl ở error.category (code giữ rawCode) — downstream Nowing/ChainLens đọc field nào?
