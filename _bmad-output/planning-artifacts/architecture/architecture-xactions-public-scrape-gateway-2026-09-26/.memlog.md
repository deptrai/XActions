---
scope: Public Scrape Gateway — sync REST surface + service auth for multi-consumer (jev/nowing/chainlens/3rd-party)
purpose: platform contract — XActions as standalone scraping standard, jev as first consumer
altitude: feature
updated: 2026-09-26T17:17
---

- (constraint) POST /api/platform/:platform/scrape already runs SYNC (platform.js:389 → scrape() direct, no queue). VALID_PLATFORMS already includes reddit+pumpfun. No new endpoint needed.
- (constraint) platform.js router.use(authenticate) = user-JWT only. Machine consumers (jev backend service, Nowing) have no user session → need serviceAuth path.
- (constraint) consumer-context.js ALREADY implements X-Consumer-Id + Bearer vs XACTIONS_MCP_API_KEY, but only bound to MCP bridge — not to REST scrape route.
- (constraint) jev-trading latency NFR: <5s/token decision, <1s mint-to-queue. Fast-lane (on-chain, pumpportal WS, trade tape) must stay direct — never route through XActions.
- (constraint) jev reimplemented PumpFunCrawler in-repo (drift). Root cause = no sync REST scrape surface + no service auth. Fix gateway, not the crawler.
- (decision) AD-1: Single gateway POST /api/platform/:platform/scrape is THE public scrape contract — no per-consumer routes. Binds all consumers.
- (decision) AD-2: Two auth lanes — user-JWT (dashboard) OR serviceAuth Bearer/API-key via existing identifyConsumer seam. Service consumers get X-Consumer-Id; third-party external via x402.
- (decision) AD-3: mode:'sync'|'async' param — sync= direct-to-crawler <1.5s for lightweight; async=queue+poll for heavy (Twitter bulk). Default per-action via manifest, caller-overridable.
- (decision) AD-4: Latency boundary is architectural, not advisory — sub-second data (on-chain/trade/mint) is consumer-owned; XActions serves seconds-to-minutes social/auth-gated scraping only.
- (decision) AD-5: Consumer-independent response envelope — unified Epic-20 shape {success,mode,metadata,data,preview}; consumer adapters normalize on THEIR side, never in XActions.
- (decision) AD-6: No consumer-specific fields in shared contract — crypto/jev fields live in context/data payload, never in ThinEvent/envelope core schema.
- (decision) AD-7: Rate-limit isolation per consumer-id — XActions proxy/token-bucket budget for a platform is independent of any consumer's own direct-fetch IP budget (Dexscreener dual-path).
- (assumption) [ASSUMPTION] jev will consume via gateway rather than keep own crawlers — requires sync latency <1.5s achieved on warm session; cold CF challenge may still exceed → degrade to async
- (event) spine drafted: Single-Gateway Facade, 7 ADs, 5 deferred, 3 open questions
- (decision) OQ-1 RESOLVED: 'jev' NOT added to VALID_CONSUMER_IDS. Rationale: (a) consumer list is a quota-billing boundary, not a caller whitelist — 'internal' already means unmetered/trusted; (b) jev is a trusted internal service in the same trust domain → normalize to 'internal' OR extend list via config, not hardcode. Decision: keep list frozen; jev authenticates via serviceAuth Bearer and is identified by X-Consumer-Id='jev' normalized to 'internal' for quota, OR list extended through env var so it's config not code. Prefer env-driven list.
- (decision) OQ-2 RESOLVED: sync-mode timeout for CF-gated calls = hard ceiling auto-degrade to async, NOT longer window. Rationale: jev's own holderConcentration fetch uses 2s timeout+non-fatal — jev tolerates bounded latency; a longer sync window would re-introduce the 'hang' that made them fork. sync ceiling fixed at 1.5s; breach → 202 + operationId + Retry-After, consumer polls. Never silent-hang.
- (decision) OQ-3 RESOLVED: x402 is NOT always-on for sync mode. Tiered: named consumers (Bearer) get metered-but-free quota; unknown/no-auth callers get 'internal'→anonymous free tier with aggressive rate-limit (IP+consumer bucket, tight ceiling); x402 required only when anonymous quota exhausted OR for premium/heavy actions. Prevents free-tier abuse without forcing payment on first call.
- (event) Spine finalized: 3 OQ resolved against code (OQ-1 jev stays at internal quota-tier via env-driven identity, no hardcode; OQ-2 sync ceiling 1.5s auto-degrade→async 202+operationId, never longer window; OQ-3 x402 tiered not always-on — anonymous free tier + named quota + x402 on exhaustion/premium). Reviewer Gate running: lint clean, 3 parallel lenses dispatched (rubric-walker, tech-verification, adversarial-divergence).
- (event) Reviewer Gate closed. Subagent dispatch blocked by tool sandbox; ran 3 inline reviews instead (rubric-walker + tech-verification + adversarial-divergence), all written to reviews/. 6 fixes applied: (1) x402 flagged as needing route-config extension for /api/platform/*/scrape — was 'already implemented' but didn't cover the route; (2) AD-2 pinned — consumer_id derived server-side from Bearer, X-Consumer-Id demoted to hint (kills spoofing on AD-7 quota); (3) AD-3 pinned degrade contract — 202+operationId+Retry-After is THE shape, never 200+error; (4) AD-5 pinned preview=verbatim data[0..10], summaries in metadata; (5) added action naming convention (snake_case verb_noun); (6) added Operational Envelope (SLO p99<1.5s sync, X-Request-Id/traceparent propagation, Redis≥6/PG≥14, single-Express+separate-Bull-worker). status: final.
