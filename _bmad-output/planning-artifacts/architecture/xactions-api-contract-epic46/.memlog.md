---
scope: HTTP API contract layer: OpenAPI 3.1 spec pipeline, validation, envelopes, docs UI, generated client
purpose: Invariants keeping api/ (Express), packages/api-client, and Epic 47 frontend consistent
altitude: epic
updated: 2026-09-24T03:01
---

- (constraint) INHERITED parent AD-14 (xactions-hybrid-scraping-spine): domain error envelope {code,type,message,retryAfter,suggestedAction,accountId?,platform} binds src/mcp,src/api,src/cli,error-envelope.js — HTTP layer must map, not reshape
- (constraint) INHERITED parent AD-16/AD-19 bind src/api/** (MCP/CLI surface) — distinct namespace from api/ Express backend; this spine governs api/ only
- (decision) AD-1 contract-first: Zod schemas are single source of truth; OpenAPI generated via @asteasolutions/zod-to-openapi; api/openapi.js refactored to shared builder; no parallel hand-written spec — prevents spec/validation drift
- (decision) AD-2 HTTP envelope: success {success:true,data:T}, error {success:false,error:{code,message,details?}}; JSON endpoints only — prevents ~50 mounts returning ≥4 heterogeneous shapes
- (decision) AD-3 domain->HTTP error mapping: PlatformError/AD-14 fields serialize as error.code=domain code (FB_*/XACT_*), error.type, error.details={retryAfter,suggestedAction,accountId?,platform}; HTTP layer never reshapes domain fields — prevents breaking MCP/CLI consumers parsing suggestedAction/retryAfter
- (decision) AD-4 scope by Route Inventory (epics.md Phu luc A); exclusions: POST /webhooks raw body, streaming/binary/SSE/redirect endpoints, /api/plugins/* runtime mounts
- (decision) AD-5 validation pipeline order authenticate->validate->handler; Zod covers body,query,path params,declared headers (x-session-cookie,x-payment); middleware-originated errors (429/404/413/415) pass through envelope formatter
- (decision) AD-6 canonical securitySchemes: bearerAuth(JWT), sessionCookie(apiKey header x-session-cookie or body field sessionCookie — per-route mechanism documented), x402Payment(X-PAYMENT header), optional-auth={} union; per-route mapping in spec
- (decision) AD-7 docs delivery: self-hosted swagger-ui-express at /api-docs mounted before /docs/:slug (CSP blocks CDN); Try-it-out disabled on mutation+x402 ops; openapi.json keeps CORS * and preserves x-payment-info/x-bazaar + /.well-known/x402 byte-compatible
- (decision) AD-8 client codegen: npm run generate:api-client at root package.json; reads committed openapi.json artifact or imports builder (never live HTTP); outputs packages/api-client as @xactions/api-client via root workspaces; openapi-typescript types + hand-written thin fetch wrapper; typed error union 400|401|402|429|500; PaginatedResponse<T>; dedupe vs existing Error/SuccessResponse/PaymentRequired components
- (decision) AD-9 contract honesty: unique operationId per operation mandatory; CI spec lint (@redocly/cli) + contract test >=1 endpoint per group via vitest+supertest; shared mounts dedupe paths (/api/analytics two routers; only /api/governor documented, not /governor)
- (version) verified npm 2026-09-24: zod@4.6.5, @asteasolutions/zod-to-openapi@9.1.0, swagger-ui-express@5.0.1, openapi-typescript@7.13.0, @redocly/cli@2.54.2, supertest@7.3.0
- (direction) Deferred: serverless.js serves same document with 503-caveat in info.description (no per-deployment spec); plugin schema registration mechanism (future plugin system); legacy ~810 error call-site migration sequencing (46.2 internal); spec deprecation/versioning policy beyond info.version; dashboard fetch migration (Epic 47 scope)
- (assumption) [ASSUMPTION] vitest is the contract-test runner (repo standard: vitest.config.js); supertest for HTTP assertions
- (question) OPEN: should /api/ai x402 endpoints stay in the unified spec or remain a separately-tagged document section? Current decision: merge, preserve extensions
- (decision) Paradigm: contract-first API layer — Zod schema is the single source generating runtime validation AND public OpenAPI document AND (transitively) the consumer client; name it 'schema-driven contract pipeline'
- (event) reviewer gate round 1: lint clean; rubric=FIX, reality=PASS-conditions, adversarial=FAIL(9 pairs,3 fatal), reconcile=PASS-gaps
- (decision) REVISED after gate: AD-3 details=toEnvelope() verbatim (was 4-field allowlist); AD-6 sessionCookie canonical=header only, middleware normalizes body field; AD-7 semantics-preserved not byte-compat + supportedSubmitMethods:['get'] baseline; AD-8 artifact=api/openapi.json + build:openapi + CI regen-diff + workspaces install-audit; inherited table corrected (AD-16/19 surfaces live in api/routes/, not src/api/); AD-2 adds type? + single-owner envelope.js; AD-5 authenticate=per-route scheme + named interception mechanisms
- (decision) zod@4 caveat: viralStatsStore.js uses undeclared transitive zod@3 — audit required in same PR (reality-check finding)
- (assumption) [ASSUMPTION] x402scan reads extension fields not component names — re-pointing safe; flagged by r2 as asserted-not-verified
- (decision) REVISED r2 residuals: AD-8 error union discriminated (402=PaymentRequired not ApiError); AD-6 normalize-shim scoped to sessionCookie-scheme routes only (save-session keeps it as payload); AD-1 request schemas never .strict(); optional-auth invalid creds=anonymous; non-domain type omitted; auth.js self-emitted 401/403/429 funnel to envelope.js
- (event) spine finalized — gate r2 residuals closed; status=final
- (decision) RESOLVED open question: /api/ai/* merges into unified openapi.json — x402scan contract = /.well-known/x402 artifact (generateWellKnown, ALL_PAID_RESOURCES list) + per-operation extensions, both survive superset doc; separate spec would violate AD-1 single-source and add codegen merge logic for zero consumer benefit
- (event) open questions resolved; spine complete
